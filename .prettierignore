dist
apps/server/dist-server
node_modules
coverage
*.min.js

# pnpm-lock.yaml is regenerated by Renovate (`lockFileMaintenance`) and by every
# `pnpm install`. Prettier reformats it inconsistently with pnpm's writer, which
# makes Renovate / lockfile-refresh PRs fail `pnpm format:check` in CI. Skip it.
pnpm-lock.yaml

# Native Android/iOS projects generated by Capacitor — не наш стиль,
# plus `app/src/main/assets/public/` містить копію web-bundle, яку
# prettier не повинен чіпати.
apps/mobile-shell/android
apps/mobile-shell/ios

# Autogenerated by license-checker — do not reformat
THIRD_PARTY_LICENSES.md

# Autogenerated by `pnpm api:generate-openapi` from zod-схем.
# Format match enforced by `.github/workflows/openapi-freshness.yml`
# (CI compares JSON.stringify output to committed file). Prettier would
# reflow strings and break the freshness check.
docs/api/openapi.json

# Autogenerated by `pnpm api:generate-openapi-types` (openapi-typescript).
# Drift gate (`scripts/api/check-openapi-types-fresh.mjs`) does byte-level
# compare to the openapi-typescript raw output; prettier reformatting
# would break the freshness check the same way it does for openapi.json.
packages/api-client/src/generated/openapi.d.ts

# Cursor skills / sandbox — не частина продуктового коду Hub
.agents
artifacts
mcps

# Plop Handlebars templates — `.hbs` файли містять `{{name}}`-mustache-токени
# у TypeScript-shape (наприклад, `): Promise<{{returnType}}> {`), які Prettier
# не може розпарсити (бачить `<{{` як HTML-тег і кидає SyntaxError). Шаблони
# не є виконуваним кодом і їх не треба форматувати — їх рендерять через plop.
plop-templates/**/*.hbs

# Pact contract artifacts — regenerated by `@pact-foundation/pact` mock
# server (`packages/api-client/src/__tests__/contracts/*.contract.test.ts`).
# Prettier reformatting would drift from the writer's own JSON layout,
# turning every consumer-test run into a format:check failure.
packages/api-client/pacts/
