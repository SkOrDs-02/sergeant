-- Down для 126_silpo_oauth_state.sql.
--
-- Local-only rollback (Hard Rule #4) — прод ніколи не запускає `.down.sql`.
-- Втрачаються лише незавершені авторизації в польоті (максимум 10 хвилин
-- життя): людина, що саме зараз на сторінці Сільпо, отримає `invalid_state`
-- і почне «Зв'язати Сільпо» заново. Уже видані токени, чеки й лінки — в
-- інших таблицях і не зачіпаються.

DROP TABLE IF EXISTS silpo_oauth_state;
