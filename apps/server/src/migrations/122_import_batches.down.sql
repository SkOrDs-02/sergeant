-- 122 down: import_batches.
--
-- Local-only rollback (Hard Rule #4) — прод ніколи не запускає
-- `.down.sql`.
--
-- Дані втрачаються повністю (журнал батчів масового ведення). Таблиця
-- щойно створена цією ж міграцією і в проді ще нічим не заповнена
-- (Фаза 2 коду, що в неї пише, ще немає), тож ризику втрати реальних
-- даних немає.

DROP TABLE IF EXISTS import_batches CASCADE;
