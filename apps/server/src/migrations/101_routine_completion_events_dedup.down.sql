-- Down для 101_routine_completion_events_dedup.sql
--
-- NO_ROLLBACK: цю міграцію не можна відкотити чесно. Крок 1 форварда
-- (`DELETE ... WHERE ranked.rn > 1`) незворотно видаляє рядки-дублікати —
-- жодних даних про них ніде більше не лишається, відновити нема звідки.
-- Крок 2 (перезапис `id` рядка, що вижив) технічно відновлюваний — старий
-- `id` можна перерахувати з тих самих колонок (`habit_id || '|' ||
-- date_key || '|' || occurred_at || '|' || state || '|' ||
-- COALESCE(device_id, '-')`) — але відкочувати ЛИШЕ id без повернення
-- видалених рядків дало б непослідовний проміжний стан, гірший за
-- нинішній. Тому down навмисно НЕ виконує SQL.
--
-- Production НІКОЛИ не запускає `.down.sql` (forward-only `pnpm
-- db:migrate`), а local-rollback тут означав би "змирись із втратою
-- дублікатів" — той самий стан, що forward і так лишає.
-- (due: 2026-08-18)
