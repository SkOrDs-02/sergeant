/**
 * Vision-промпт для `POST /api/finyk/import/screenshot/analyze` (скрін
 * екрана банкінгу — список транзакцій або одне push-повідомлення).
 *
 * Тримається окремим модулем-константою — той самий патерн, що
 * `finyk/receipts/prompts.ts` (SYSTEM у `nutrition/analyze-photo.ts`):
 * єдине джерело тексту, яке майбутній зоровий стенд (`pnpm eval:vision`)
 * міг би імпортувати напряму замість копії, що розходиться з продом.
 *
 * Формат полів — snake_case (`amount_kopiykas`) навмисно дзеркалить
 * колонки/wire-конвенцію решти чек-скан LLM-контрактів цього модуля —
 * це ВНУТРІШНІЙ контракт (сирий текст LLM), не публічна wire-форма; draft,
 * який бачить клієнт, camelCase (`ImportScreenshotDraftSchema`,
 * `@sergeant/shared`) — мапінг у `screenshotAnalyze.ts#normalizeImportScreenshotResult`.
 *
 * Спека (`docs/90-work/planning/specs/receipt-scan.md` § «Фаза 2» →
 * «Рішення дизайну Фази 2» → «Скрін банкінгу — це транзакції без позицій,
 * НЕ чек») зобовʼязує промпт:
 *   - ігнорувати рядки балансу/«Доступно»/лімітів;
 *   - не вигадувати рік, якщо його немає на скріні (типова ситуація для
 *     банк-апок) — брати поточний;
 *   - суми — ЦІЛІ kopiykas.
 */
export const IMPORT_SCREENSHOT_VISION_SYSTEM_PROMPT = `Ти розпізнаєш скріншот екрана банківського додатку — список транзакцій або одне push-повідомлення банку про операцію. Відповідай ТІЛЬКИ українською.
Поверни ТІЛЬКИ валідний JSON без markdown і без жодного тексту навколо.

Формат:
{
  "doc_type": "bank_screenshot" | "receipt" | "other",
  "bank": string|null,        // назва банку, якщо впізнав логотип/бренд екрана, інакше null
  "rows": [
    {
      "date": string,          // дата операції у форматі YYYY-MM-DD
      "time": string|null,     // час операції HH:MM (24-годинний), null якщо не читається
      "amount_kopiykas": number, // сума операції в КОПІЙКАХ, ціле додатне число (847.50 грн = 84750)
      "direction": "expense" | "income",
      "description": string,   // опис/мерчант/контрагент, як на екрані
      "failed": boolean,        // true, якщо рядок позначено як НЕВДАЛУ операцію («Недостатньо коштів на картці», «Відхилено», «Скасовано», «Не пройшла» тощо)
      "currency": string,       // код валюти суми рядка: "UAH" для ₴/грн; "EUR" для €, "USD" для $, "PLN" для zł і т.д. — сервер відфільтрує не-гривневі
      "confidence": number      // 0..1, наскільки ти впевнений у сумі й напрямку САМЕ цього рядка
    }
  ]
}

Визначення "doc_type":
- "bank_screenshot" — це скрін списку транзакцій банк-апки АБО одне push-повідомлення про операцію. Заповни "rows".
- "receipt" — це фото товарного чека з каси магазину (не банк-апка) — "rows" лиши порожнім масивом.
- "other" — щось інше (не банківський скрін і не чек) — "rows" лиши порожнім масивом.

ВАЖЛИВО про рядки:
- НЕ додавай у "rows" рядки балансу, «Доступно», «Ліміт», «Залишок після операції» чи будь-які підсумкові/лімітні цифри — це не транзакції, а стан рахунку.
- НЕВДАЛІ операції (позначки «Недостатньо коштів на картці», «Відхилено», «Скасовано», «Не пройшла») — став їм "failed": true; гроші за ними НЕ рухались, сервер їх відфільтрує. Для звичайних рядків — "failed": false.
- Рядки з сумами НЕ в гривнях (€, $, zł, £ чи інша валюта біля числа) — став їм чесний код у "currency" (імпорт працює лише з UAH; не-гривневі рядки сервер відфільтрує за цим полем).
- "description" — лише мерчант/контрагент/назва операції з рядка. НЕ включай рекламні чи жартівливі підписи застосунку під рядком (типу «Баланс схуд. Підгодуємо?», «Картка муркоче про поповнення?») — це не частина опису операції.
- "direction": "expense" для списання (мінус, часто червоний колір/стрілка вниз), "income" для надходження (плюс, часто зелений колір/стрілка вгору). Знак у "amount_kopiykas" НЕ кодуй — сума завжди додатна, напрям лише в "direction".
- "amount_kopiykas" — ціле число копійок; ніколи гривні, ніколи дробове.
- "date" — YYYY-MM-DD. Якщо на екрані немає року (типова ситуація для банк-апок — вони часто показують лише день і місяць) — візьми сьогоднішню дату з повідомлення користувача і обери ОСТАННІЙ такий день-місяць, що НЕ пізніший за сьогодні (скріни показують минулі операції, не майбутні). НЕ вигадуй рік навмання.
- "time" — HH:MM (24-годинний), null якщо не читається чи відсутній (push-повідомлення іноді не несуть окремої мітки часу).
- "confidence" — 0..1 на кожен рядок окремо, не загальна оцінка скріна.
- Не можеш розібрати жодного рядка — постав "rows": [], але "doc_type" все одно визнач за загальним виглядом екрана.
`;

/**
 * `todayKyiv` (`YYYY-MM-DD`, київський день) — ЯКІР для дат без року.
 * Live-бенч 2026-08-18 на реальних скрінах mono: без якоря всі три
 * протестовані моделі (gemini-2.5-flash-lite/-flash, haiku-4.5) впевнено
 * галюцинували рік («16 серпня» → 2023 чи 2024) — інструкція «використай
 * поточний рік» без самої дати нездійсненна, бо модель не знає, який
 * сьогодні день.
 */
export function buildImportScreenshotUserPrompt(todayKyiv: string): string {
  return `Сьогодні ${todayKyiv}. Розпізнай цей скріншот банківського додатку і поверни лише JSON за описаним форматом, без жодного іншого тексту.`;
}
