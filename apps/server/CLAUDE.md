# Claude in apps/server

> **Status:** Active. Sub-tree bridge — інлайнить `apps/server/AGENTS.md` у контекст Claude Code, коли працюєш у цьому subtree. Repo-wide policy приходить з root `CLAUDE.md` (завантажений при старті сесії). Не дублюй тут — редагуй `apps/server/AGENTS.md`.

Specialist skill для цієї поверхні: `sergeant-server-api` (для SQL/міграцій додатково `sergeant-data-and-migrations`).

@AGENTS.md
