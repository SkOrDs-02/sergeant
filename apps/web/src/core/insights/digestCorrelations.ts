import { getKyivDayKey } from "@shared/lib/time/kyivTime";
import {
  buildDailySeries,
  computePairwiseCorrelations,
  type DailyMetric,
  type DailySeries,
} from "../lib/chatActions/crossActions/dailySeries";

/**
 * Крос-модульні кореляції для weekly-digest → памʼять коуча (WP3).
 *
 * Рахуємо КОДОМ (не LLM) фіксований набір змістовних пар за 60 днів, переюзаючи
 * той самий обчислювальний примітив, що й chat-тул `get_daily_series`
 * (`buildDailySeries` + `computePairwiseCorrelations`). Повертаємо ≤3
 * людські one-liner-и лише для статистично помітних пар — коуч потім вставляє
 * їх у промпт без жодного додаткового виклику моделі.
 *
 * Anti-scope: це НЕ агентизує коуча. Коуч лишається single-shot без tool-use;
 * ми лише даємо йому якісніший snapshot.
 */

// Exported (2026-08-05, P2 anti-slop форма звʼязку — `CrossModuleLinkCard`):
// `crossModuleLinkTiers.ts` derives its 3-ступенева візуальна градація
// впевненості з ЦИХ САМИХ порогів замість того, щоб вигадувати нові —
// «епістемічний стандарт звʼязків» (product-overview.md §6) вимагає, щоб
// поріг мовчання був один на весь продукт, а не по одному на кожну поверхню.
export const WINDOW_DAYS = 60;
export const NOTABLE_R = 0.4;

/**
 * Мінімум спільних днів, щоб пара взагалі заговорила.
 *
 * Порогом 5 продукт жив на час закритої бети (рішення власника 2026-08-05):
 * на малих даних треба було показувати бодай щось, щоб побачити реакцію.
 * Умовою зняття було саме закриття бети — виконано.
 *
 * Чому 5 було боргом, а не нормою: на пʼяти точках `|r| = 0.4` трапляється
 * на випадкових даних приблизно в половині випадків, тобто перший ступінь
 * стояв на доказі рівня підкидання монети. Назва «Поки що збіг»
 * (`crossModuleLinkTiers.ts`) була чесною, але доказу не посилювала. Набір
 * пар розширено 2026-08-05 з 9 до 15, і це множило проблему: більше
 * перевірених гіпотез — вищий шанс, що хоч одна перетне поріг випадково.
 */
export const MIN_N = 10;
const MAX_LINES = 3;

interface PairPhrase {
  a: DailyMetric;
  b: DailyMetric;
  pos: string;
  neg: string;
}

// Курований набір пар, де звʼязок має продуктовий сенс. Порядок метрик у
// `METRICS` нижче не має значення — пара шукається в обох напрямках.
const PAIRS: readonly PairPhrase[] = [
  {
    a: "workout_volume",
    b: "spending",
    pos: "у дні тренувань ти витрачаєш більше",
    neg: "у дні тренувань ти витрачаєш менше",
  },
  {
    a: "habit_rate",
    b: "kcal",
    pos: "коли тримаєш звички, їси більше",
    neg: "коли тримаєш звички, їси менше",
  },
  {
    a: "protein",
    b: "workout_volume",
    pos: "коли їси більше білка, тренуєшся важче",
    neg: "коли їси більше білка, тренуєшся легше",
  },
  {
    a: "weight",
    b: "kcal",
    pos: "вага росте разом із калоріями",
    neg: "більше калорій збігається з нижчою вагою",
  },
  {
    a: "workout_volume",
    b: "wellbeing",
    pos: "після важчих тренувань почуваєшся краще",
    neg: "після важчих тренувань почуваєшся гірше",
  },
  {
    a: "habit_rate",
    b: "wellbeing",
    pos: "коли тримаєш звички, почуваєшся краще",
    neg: "коли тримаєш звички, почуваєшся гірше",
  },
  // Пари з `alcohol_spending` стали можливими лише після чекового спліту
  // (Silpo трек F): доти алкоголь був невіддільною частиною `groceries`,
  // тож поставити це питання не було на чому. Формулювання — про збіг, не
  // про причину: 60 днів і `r` не доводять причинності, і копія не має
  // вдавати, ніби доводять.
  {
    a: "alcohol_spending",
    b: "wellbeing",
    pos: "у дні з алкоголем самопочуття вище",
    neg: "у дні з алкоголем самопочуття нижче",
  },
  {
    a: "alcohol_spending",
    b: "kcal",
    pos: "дні з алкоголем збігаються з більшою калорійністю",
    neg: "дні з алкоголем збігаються з меншою калорійністю",
  },
  {
    a: "alcohol_spending",
    b: "habit_rate",
    pos: "у дні з алкоголем звички тримаються краще",
    neg: "у дні з алкоголем звички тримаються гірше",
  },
  // Тютюн - другий чековий клас після алкоголю (spec silpo § трек F).
  // Мапінг у `receiptSplitSuggestion.ts` ловить і назви («цигарк», «тютюн»,
  // «снюс»), і бренди нагріваних стіків (heets / terea / glo), тож витрата
  // тримається окремо від решти покупок у супермаркеті.
  //
  // Пари ті самі, що в алкоголю, крім калорій: цигарки не їдять, і зв'язок
  // «курив - більше з'їв» був би вигаданою гіпотезою заради симетрії.
  // Замість неї - обсяг тренувань: у курця це найпряміший фізичний слід.
  {
    a: "smoking_spending",
    b: "wellbeing",
    pos: "у дні з цигарками самопочуття вище",
    neg: "у дні з цигарками самопочуття нижче",
  },
  {
    a: "smoking_spending",
    b: "habit_rate",
    pos: "у дні з цигарками звички тримаються краще",
    neg: "у дні з цигарками звички тримаються гірше",
  },
  {
    a: "smoking_spending",
    b: "workout_volume",
    pos: "у дні з цигарками обсяг тренувань вищий",
    neg: "у дні з цигарками обсяг тренувань нижчий",
  },
  {
    a: "wellbeing",
    b: "spending",
    pos: "коли самопочуття краще, ти витрачаєш більше",
    neg: "коли самопочуття краще, ти витрачаєш менше",
  },
  {
    a: "workout_volume",
    b: "water",
    pos: "у дні тренувань ти пʼєш більше води",
    neg: "у дні тренувань ти пʼєш менше води",
  },
  /*
   * Було `workouts × habit_rate` — замінено на обʼєм 2026-08-05.
   *
   * `workouts` існує лише в дні з тренуванням, і там воно майже завжди
   * дорівнює 1: розкиду в змінній практично немає, тож кореляція на ній —
   * шум, який при `MIN_N = 5` регулярно перетинав би поріг. Обʼєм тренування
   * у тих самих днях має справжній розкид.
   */
  {
    a: "workout_volume",
    b: "habit_rate",
    pos: "коли тренуєшся важче, краще тримаєш звички",
    neg: "коли тренуєшся важче, гірше тримаєш звички",
  },

  // ─── Додано 2026-08-05: покриття всіх шести комбінацій модулів ──────────
  // До цього Фінік × Їжа не мала жодної пари — код бачив звʼязок, але не
  // мав речення, щоб його сказати.
  {
    a: "spending",
    b: "kcal",
    pos: "коли витрачаєш більше, їси більше",
    neg: "коли витрачаєш більше, їси менше",
  },
  {
    a: "spending",
    b: "habit_rate",
    pos: "коли тримаєш звички, витрачаєш більше",
    neg: "коли тримаєш звички, витрачаєш менше",
  },
  {
    a: "workout_volume",
    b: "kcal",
    pos: "у дні важчих тренувань ти їси більше",
    neg: "у дні важчих тренувань ти їси менше",
  },
  {
    a: "kcal",
    b: "wellbeing",
    pos: "у дні, коли їси більше, самопочуття краще",
    neg: "у дні, коли їси більше, самопочуття гірше",
  },
  {
    a: "water",
    b: "wellbeing",
    pos: "коли пʼєш більше води, почуваєшся краще",
    neg: "коли пʼєш більше води, почуваєшся гірше",
  },
  {
    a: "weight",
    b: "habit_rate",
    pos: "коли тримаєш звички, вага вища",
    neg: "коли тримаєш звички, вага нижча",
  },
];

/**
 * Метрики курованих пар без фраз — щоб стан мовчання міг показати прогрес по
 * тих самих парах, які колись заговорять, а не по довільній комбінації
 * метрик.
 */
export const CURATED_PAIRS: ReadonlyArray<{
  a: DailyMetric;
  b: DailyMetric;
}> = PAIRS.map((p) => ({ a: p.a, b: p.b }));

const METRICS: DailyMetric[] = [
  "spending",
  "kcal",
  "protein",
  "workout_volume",
  "weight",
  "habit_rate",
  "wellbeing",
  "water",
  "workouts",
  "alcohol_spending",
  "smoking_spending",
];

/**
 * Помітна пара в структурованому вигляді — та сама знахідка, що й рядок
 * `correlationsFromSeries`, але до форматування в текст.
 *
 * AI-CONTEXT: винесено 2026-08-05, коли `CrossModuleLinkCard` (P2 анти-слоп)
 * отримав власну секцію на `/insights`. Картці потрібні `n` і `r` окремими
 * числами (з них рахується ступінь впевненості) плюс метрики полюсів (з них
 * будуються дві осі) — тобто рівно те, що стара функція склеювала в рядок і
 * викидала. Щоб поріг мовчання лишався ОДИН на продукт, обидва споживачі
 * читають цей самий список, а не два паралельні обчислення.
 */
export interface NotablePair {
  a: DailyMetric;
  b: DailyMetric;
  /** Людська фраза, уже обрана за знаком `pearson` (`pos` або `neg`). */
  phrase: string;
  /** Спільні дні з обома метриками (pairwise-complete). */
  n: number;
  pearson: number;
}

/**
 * Чиста частина: з уже побудованих рядів дістає ВСІ помітні пари,
 * відсортовані за |r|. Виокремлено для юніт-тестів (не залежить від
 * storage/годинника). Обрізання під конкретну поверхню — на боці викликача.
 */
export function notablePairsFromSeries(series: DailySeries): NotablePair[] {
  const byPair = new Map(
    computePairwiseCorrelations(series).map((c) => [`${c.a}|${c.b}`, c]),
  );

  const found: NotablePair[] = [];
  for (const p of PAIRS) {
    const c = byPair.get(`${p.a}|${p.b}`) ?? byPair.get(`${p.b}|${p.a}`);
    if (!c || c.n < MIN_N || !Number.isFinite(c.pearson)) continue;
    if (Math.abs(c.pearson) < NOTABLE_R) continue;
    found.push({
      a: p.a,
      b: p.b,
      phrase: c.pearson > 0 ? p.pos : p.neg,
      n: c.n,
      pearson: c.pearson,
    });
  }

  return found.sort((x, y) => Math.abs(y.pearson) - Math.abs(x.pearson));
}

/**
 * Чиста частина: з уже побудованих рядів дістає до 3 one-liner-ів про помітні
 * пари, відсортовані за |r|. Виокремлено для юніт-тестів (не залежить від
 * storage/годинника).
 */
export function correlationsFromSeries(series: DailySeries): string[] {
  return notablePairsFromSeries(series)
    .slice(0, MAX_LINES)
    .map((p) => `${p.phrase} (r=${p.pearson.toFixed(2)}, ${p.n} дн)`);
}

/**
 * Зсув ключа дня `YYYY-MM-DD` на N календарних днів.
 *
 * AI-CONTEXT: раніше початок вікна рахувався як `getKyivDayKey(now - 59 *
 * DAY_MS)`, тобто відніманням МІЛІСЕКУНД від моменту часу. Це не те саме, що
 * відняти 59 календарних днів: Київ переходить на літній час, і на переході
 * доба триває 23 або 25 годин. Вікно, яке перетинає перехід, давало 61 ключ
 * замість 60 — наприклад, для моменту одразу після півночі 1 квітня початок
 * випадав на 31 січня замість 1 лютого.
 *
 * Ключ дня — це вже календарна дата без зони, тож арифметика робиться на
 * ній самій через UTC-полудень: UTC не має переходів, і зсув на добу завжди
 * рівно одна доба.
 */
function shiftDayKey(dayKey: string, deltaDays: number): string {
  // UTC-полудень навмисно: це не читання «зараз», а чиста календарна
  // арифметика над уже київським ключем дня.
  const d = new Date(`${dayKey}T12:00:00.000Z`);
  d.setUTCDate(d.getUTCDate() + deltaDays);
  return d.toISOString().slice(0, 10);
}

/**
 * Ряди за вікно аналізу — спільна основа для дайджест-рядків і для секції
 * звʼязків на `/insights`. Обидві поверхні мусять дивитись на ОДНЕ вікно,
 * інакше та сама пара показала б різні `n` у різних місцях.
 */
export function buildCrossModuleSeries(now: number = Date.now()): DailySeries {
  const to = getKyivDayKey(now);
  return buildDailySeries(METRICS, {
    from: shiftDayKey(to, -(WINDOW_DAYS - 1)),
    to,
  });
}

/**
 * Повертає до 3 one-liner-ів про помітні крос-модульні звʼязки за останні 60
 * днів, відсортовані за |r|. Порожній масив, якщо нічого не набралося.
 */
export function buildDigestCorrelations(now: number = Date.now()): string[] {
  return correlationsFromSeries(buildCrossModuleSeries(now));
}
