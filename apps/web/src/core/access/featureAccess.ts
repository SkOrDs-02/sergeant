/**
 * Last validated: 2026-09-11
 * Status: Active
 *
 * **Один предикат «чи пускати в дію» замість дев'яти різних `if`.**
 *
 * Поставка 2 з двох (рішення власника 2026-09-11). Поставка 1 —
 * `shared/lib/api/accessDenial.ts` — назвала причину відмови ТИПОМ; тут
 * та сама причина рахується ДО запиту, тож заборонений сценарій не
 * стартує взагалі.
 *
 * AI-CONTEXT: до цього гейт «до старту» жив рівно в трьох місцях (чат,
 * чек-скан, фото їжі), і кожен мав свій предикат, свій текст і свою
 * форму подачі. Шість інших AI-поверхонь пускали аноніма в дію і
 * показували 401 уже після того, як людина щось вклала. Виправляти це
 * шостим `if` означало б закріпити причину, а не прибрати її.
 *
 * AI-DANGER: **предикат навмисно НЕ намагається вгадати тариф і квоту.**
 * Клієнт не знає їх достовірно до запиту: знімок білінгу може ще не
 * приїхати, а лічильник квоти живе на сервері. Вгадування тут дало б
 * гірший клас помилки — відмову людині, якій насправді можна. Тому
 * до старту перевіряємо лише те, що клієнт знає точно: мережу і
 * наявність акаунта. «Не той план» (402 + `PLAN_REQUIRED`) і
 * «вичерпана квота» (429 + `AI_QUOTA`) приходять із сервера і
 * розбираються `resolveDenial` — у ТУ САМУ структуру `AccessDenial`,
 * тож подача pre-gate і подача помилки після старту збігаються за
 * побудовою, а не за домовленістю.
 */
import type { AccessDenial } from "@shared/lib/api/accessDenial";

/**
 * Поверхні, які не працюють без акаунта. Список — не стиль, а факт
 * сервера: кожна з цих ручок стоїть за `requireSession()`.
 *
 * Три перші вже мали власний гейт до цієї поставки й переведені на
 * спільний предикат; решта шість гейта не мали взагалі.
 */
export type GatedFeature =
  | "chat"
  | "receipt-scan"
  | "meal-photo"
  | "week-plan"
  | "day-plan"
  | "shopping-list"
  | "recipes"
  | "pantry-parse"
  | "cloud-backup"
  | "voice-input";

/** Те, що клієнт знає про себе достовірно ДО запиту. */
export interface AccessContext {
  /** Жива сесія Better Auth. Синтетичні id (`local-anon`, `demo-local`) — не акаунт. */
  hasAccount: boolean;
  /** `useOnlineStatus()` — не голий `navigator.onLine`, він бреше на iOS. */
  online: boolean;
}

/**
 * Причина, з якої фічу не можна запускати просто зараз, або `null`,
 * якщо можна.
 *
 * Порядок перевірок не довільний: **офлайн сильніший за вхід**. Людині
 * без мережі пропозиція увійти марна — вхід теж потребує мережі, тож
 * вона б уперлась у другу стіну одразу після першої.
 */
export function resolveFeatureDenial(
  _feature: GatedFeature,
  ctx: AccessContext,
): AccessDenial | null {
  if (!ctx.online) return { reason: "offline" };
  if (!ctx.hasAccount) return { reason: "sign-in-required" };
  return null;
}
