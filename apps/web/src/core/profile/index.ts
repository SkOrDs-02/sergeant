/**
 * Public barrel for the web profile surface (ProfilePage + biometric hooks).
 *
 * @scaffolded
 * @nextStep Migrate `apps/web/src/core/app/router.tsx` and any other call-site
 *   from `./core/profile/ProfilePage` deep-import to barrel `@core/profile`.
 *   Tracked in dead-code roast 2026-05-13.
 */
export { ProfilePage } from "./ProfilePage";
