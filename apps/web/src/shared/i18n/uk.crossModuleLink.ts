/** @status Active */

/**
 * Копія `CrossModuleLinkCard` (P2 анти-слоп плану, `docs/05-design/design/
 * anti-slop-strategy.md` §5) — форма крос-модульного зв'язку.
 *
 * AI-CONTEXT: винесено одразу в окремий файл 2026-08-05, а не додано в
 * `uk.ts` — той самий прецедент, що `uk.privacy.ts` (2026-07-25): каталог
 * росте разом із продуктом, тож ділиться по розділах, а не ратчетиться
 * ліміт `max-lines: 600` (Hard Rule #18).
 *
 * Канон product-overview §6 — «градація впевненості + право мовчати»:
 * «закономірностей не помічено» це легітимна відповідь із власним
 * виглядом, а не порожній список, тому копія тут пояснює мовчання, а не
 * вибачається за нього.
 */
export const crossModuleLinkMessages = {
  silentTitle: "Поки що закономірностей не бачу",
  silentBody:
    "Це не помилка — просто даних ще замало, щоб я міг стверджувати. Не вигадуватиму зв'язок, якого не видно.",
  /**
   * «3 з 5 спостережень» — прогрес до порога мовчання `MIN_N`. Саме слово
   * тримає плюралізацію (`observationsWordUk` з `crossModuleLinkTiers.ts`,
   * `Intl.PluralRules('uk-UA')`), не фіксовану форму — `MIN_N` сьогодні
   * дорівнює 5 («спостережень»), але рядок не має мовчки ламатись, якщо
   * поріг колись зміниться.
   */
  progressOf: "з",
};
