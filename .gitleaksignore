# Suppress false-positive gitleaks findings.
#
# Format: <commit-sha>:<path>:<rule-id>:<line>
#
# When adding entries, leave a comment explaining WHY the value is not a
# secret. Public identifiers (Railway service IDs, Vercel project IDs,
# Sentry DSNs without a private token, etc.) commonly trip the
# `generic-api-key` heuristic when they appear in `KEY="value"` shell
# assignments — those are documented IDs, not credentials, and live in
# plain text in `docs/03-operations/deploy/console.md` and similar.

# docs/03-operations/deploy/monorepo-deploy-filtering.md (commit fcd3c267) carried the
# Railway service ID `accea0e9-…` inside a shell-assignment example line
# `API_SVC="<uuid>"`. Tripped `generic-api-key` because of the high-entropy
# UUID following an ALL_CAPS name + `=`. The ID is a public Railway service
# identifier (also listed in plain text in docs/03-operations/deploy/console.md
# §«Project / environment»), not a credential. Follow-up commit 2ce3ad40
# replaced the snippet with `$SERVICE_ID` placeholders so future commits
# don't carry the pattern.
fcd3c267e1ef00467d1de18d64bbc5b2adb2fba2:docs/03-operations/deploy/monorepo-deploy-filtering.md:generic-api-key:134

# apps/mobile/e2e/auth-flows.e2e.ts (commit 18a6dcab) introduced a static
# Detox sign-up fixture password constant (`SIGN_UP_PASSWORD`). It is a
# non-secret test value fed to the mock auth interceptor (which accepts any
# value) — not a credential. The current HEAD already carries an inline
# `// gitleaks:allow` on that line; this fingerprint suppresses the historical
# introducing commit, which the inline marker cannot retroactively cover.
# (The literal value is intentionally not repeated here so this ignore comment
# does not itself trip the generic-api-key heuristic.)
18a6dcab46540257ecbeb9fa623aca520203b29f:apps/mobile/e2e/auth-flows.e2e.ts:generic-api-key:53

# apps/web/src/modules/finyk/pages/overview/MonthStrip.tsx (commit decc1928)
# carried `STRIP_HINT_DISMISSED_KEY = "finyk_month_strip_hint_dismissed_v1"`:
# ALL_CAPS name that ends in `_KEY` + a long snake_case string tripped
# `generic-api-key`. It is a localStorage slot name (the same family as
# `finyk_assets_mono_cards_open_v1`), not a credential. Follow-up commit
# renamed the constant to `STRIP_HINT_DISMISSED_SLOT` so future commits
# don't carry the pattern.
decc1928a6b2e2e4f9949835d9b17863318befb5:apps/web/src/modules/finyk/pages/overview/MonthStrip.tsx:generic-api-key:177
