# Module ownership map

> **Last touched:** 2026-09-02 by @claude. **Next review:** 2026-12-08.
> **Status:** Active

> Per-path ownership, test stack, RQ keys factory, and conventions. Quick look-up before editing. Compact summary table lives in [`AGENTS.md § Module ownership map`](../../../AGENTS.md#module-ownership-map); deep per-path table is here so the Stack-pulse PR-04 secondary-column contract and the per-module ownership map stay close to one another. Механічного гейта більше немає — CODEOWNERS + coverage-перевірку прибрано [ADR-0082](../../04-governance/adr/0082-private-storage-repo-posture.md) §3.

¹ **Secondary** is the bus-factor backup reviewer. Real GitHub handles like `@alice` are preferred, but during the rollout we use placeholders such as `TBD (frontend-engineer)`. Порожні клітинки — порушення контракту PR-04, але перевіряються тепер очима рев'юера, а не гейтом (див. [ADR-0082](../../04-governance/adr/0082-private-storage-repo-posture.md) §3).

> **AI-CONTEXT:** All `Secondary` columns currently read `TBD (<role>`) — known bus-factor gap (Stack-pulse PR-04). Until real GitHub handles are assigned, every path has exactly one reviewer. Spawn a reconciliation task from the recurring docs-drift workflow to assign handles, starting with highest-velocity paths (`apps/web/src/core/**`, `apps/web/src/modules/finyk/**`, `apps/server/src/modules/**`).

## Apps

| Path                                   | Owner        | Secondary ¹             | Test stack                              | RQ keys factory                                                                                                          | Notes                                                                                                                                                                                       |
| -------------------------------------- | ------------ | ----------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/src/modules/finyk/**`        | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest + MSW + RTL                      | `finykKeys`                                                                                                              | Tailwind + SQLite-WASM primary state (sync v2). UI prefs (hidden txs, splits) — `localStorage`. Mono webhooks → `monoWebhook*` keys.                                                        |
| `apps/web/src/modules/fizruk/**`       | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest + MSW + RTL                      | (none — local-first; no `fizrukKeys` factory)                                                                            | Workouts/sets local-first (SQLite-WASM + sync v2 outbox). Intentionally no RQ factory — see `fizrukQueryKeyContainment.test.ts`.                                                            |
| `apps/web/src/modules/nutrition/**`    | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest + MSW + RTL                      | `nutritionKeys`                                                                                                          | OFF = OpenFoodFacts; barcode scans share cache key with meal-sheet.                                                                                                                         |
| `apps/web/src/modules/routine/**`      | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest + RTL                            | (local-first)                                                                                                            | Habits + streaks; day-key береться з годинника пристрою ([ADR-0078](../../04-governance/adr/0078-day-boundary-device-local.md), [Domain invariants](./domain-invariants.md)).               |
| `apps/web/src/core/**`                 | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest + RTL + (MSW for fetch)          | `hubKeys`, `coachKeys`, `chatKeys`, `digestKeys`, `syncKeys`, `pushKeys`, `strategicKeys`, `billingKeys`, `aiMemoryKeys` | HubChat, OnboardingWizard, dashboard, sync v2 status (`useSyncStatus`). Quick actions registry lives here. Factories — `apps/web/src/shared/lib/api/queryKeys.ts`.                          |
| `apps/web/src/core/lib/chatActions/**` | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest + RTL                            | n/a                                                                                                                      | HubChat tool handlers. Повертають `string` для `tool_result`. Пишуть у localStorage тільки через `ls`/`lsSet`. Тест: happy path + error path кожного handler-а.                             |
| `apps/web/src/shared/**`               | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest                                  | factories defined here                                                                                                   | Pure utils. No React.                                                                                                                                                                       |
| `apps/landing/**`                      | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest                                  | n/a                                                                                                                      | Standalone Vercel marketing landing. Uses shared analytics event names; does not share the `apps/web` runtime bundle.                                                                       |
| `apps/server/src/modules/**`           | `@SkOrDs-02` | TBD (backend-engineer)  | Vitest + Testcontainers (real Postgres) | n/a                                                                                                                      | Always coerce bigint→number in serializers ([Rule #1](../../04-governance/governance/rules/01-db-types-coerce-bigint-to-number.md)). Update `api-client` types.                             |
| `apps/server/src/modules/chat/**`      | `@SkOrDs-02` | TBD (backend-engineer)  | Vitest                                  | n/a                                                                                                                      | Anthropic tool defs split per domain in `toolDefs/`. See [`module-structure.md`](./module-structure.md).                                                                                    |
| `apps/server/src/migrations/**`        | `@SkOrDs-02` | TBD (data-engineer)     | n/a                                     | n/a                                                                                                                      | Sequential `NNN_*.sql` (currently 001–103 forward migrations). No gaps. Two-phase for DROP — see [Rule #4](../../04-governance/governance/rules/04-sql-migrations-sequential-two-phase.md). |
| `apps/mobile/src/core/**`              | `@SkOrDs-02` | TBD (mobile-engineer)   | Jest                                    | (mobile RQ uses module-local keys)                                                                                       | NativeWind (not Tailwind). MMKV (not localStorage). No DOM.                                                                                                                                 |
| `apps/mobile/app/**`                   | `@SkOrDs-02` | TBD (mobile-engineer)   | Jest                                    | n/a                                                                                                                      | Expo Router routes. Each `_layout.tsx` is a navigator.                                                                                                                                      |
| `apps/mobile-shell/**`                 | `@SkOrDs-02` | TBD (mobile-engineer)   | Vitest                                  | n/a                                                                                                                      | Capacitor wrapper around `apps/web` + native bridges у `src/` (platform, auth-storage, barcodeNative, pushNative), які `apps/web` імпортує як `@sergeant/mobile-shell`.                     |

## Packages

| Path                                                  | Owner        | Secondary ¹             | Test stack    | RQ keys factory | Notes                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------- | ------------ | ----------------------- | ------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/shared/**`                                  | `@SkOrDs-02` | TBD (any-engineer)      | Vitest        | n/a             | Zod schemas, types, business logic. Used by all apps — change with care.                                                                                                                                                                                                                |
| `packages/api-client/**`                              | `@SkOrDs-02` | TBD (backend-engineer)  | Vitest        | n/a             | HTTP clients + types. Must mirror `apps/server/src/modules/*` response shapes.                                                                                                                                                                                                          |
| `packages/insights/**`                                | `@SkOrDs-02` | TBD (any-engineer)      | Vitest        | n/a             | Cross-module analytics. Pure functions over normalized data.                                                                                                                                                                                                                            |
| `packages/{finyk,fizruk,nutrition,routine}-domain/**` | `@SkOrDs-02` | TBD (any-engineer)      | Vitest        | n/a             | Domain logic shared web ↔ mobile (e.g., kcal math, budget computations).                                                                                                                                                                                                                |
| `packages/db-schema/**`                               | `@SkOrDs-02` | TBD (data-engineer)     | Vitest        | n/a             | Drizzle ORM schemas (Postgres + SQLite) and the migration runner used by `apps/server`. Schema changes pair with a new SQL migration in `apps/server/src/migrations/`.                                                                                                                  |
| `packages/config/**`                                  | `@SkOrDs-02` | TBD (any-engineer)      | n/a           | n/a             | Shared TypeScript/build config consumed by all workspaces.                                                                                                                                                                                                                              |
| `packages/design-tokens/**`                           | `@SkOrDs-02` | TBD (frontend-engineer) | Vitest        | n/a             | Shared design tokens + Tailwind preset for web and mobile. Source of truth for color/spacing/typography.                                                                                                                                                                                |
| `packages/eslint-plugin-sergeant-design/**`           | `@SkOrDs-02` | TBD (frontend-engineer) | `node --test` | n/a             | Custom ESLint rules enforcing runtime/security/storage/API invariants (`rq-keys-only-from-factory`, `no-raw-local-storage`, `no-raw-req-in-pino-log`, `ai-marker-syntax`, …). Естетичні AST-правила retired ADR-0081. Tests are `.mjs` files under `__tests__/`, run via `node --test`. |

## Ops surfaces

| Path                      | Owner        | Secondary ¹        | Notes                                                                                                                            |
| ------------------------- | ------------ | ------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| `tools/tsconfig-guard/**` | `@SkOrDs-02` | TBD (any-engineer) | Strict-family flag guard (Hard Rule #19). Allowlist with expiry/owner; gates `pnpm lint`.                                        |
| `scripts/**`              | `@SkOrDs-02` | TBD (any-engineer) | Governance / docs / API / CI helpers. See [`docs/04-governance/governance/README.md`](../../04-governance/governance/README.md). |

## See also

- [`repo-map.md`](./repo-map.md) — per-app stack matrix + per-package purpose.
- [`module-structure.md`](./module-structure.md) — canonical layout of `apps/{web,mobile}/src/modules/<domain>/` + per-module deviations.
- [`docs/04-governance/governance/rules/`](../../04-governance/governance/rules) — per-rule canonical bodies referenced by the table notes.
