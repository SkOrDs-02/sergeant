# SPEC: cutover читачів на журнал цілей КБЖВ (стадія 3)

> **Last touched:** 2026-09-03 by @claude (звірка з `origin/main`: PR-1 у `main`, PR-2/PR-3 ще ні; знято посилання на секцію, якої не існує). **Next review:** 2026-12-02.
> **Status:** In progress — PR-1 у `main` ([#1044](https://github.com/SkOrDs-02/sergeant/pull/1044), шлях запису доведено тестом); лишились PR-2 (ряд цілей у графіках, `targetKcalByDay`) і PR-3 (cutover девʼяти читачів + `METRICS_VERSION` 12 → 13). Деталі — § Стан виконання.

<!-- Спека самодостатня: виконавець у свіжій сесії реалізує стадію 3, читаючи
лише її. Продуктові рішення НЕ виводяться тут заново - вони ратифіковані
[ADR-0091](../../../04-governance/adr/0091-goal-history-not-repainted.md)
(статус `Accepted`, 2026-09-02). Ця спека перекладає їх у план змін і додає
інженерні рішення, яких ADR не робив. Єдине питання, що лишалось за
founder-ом (маркер реконструйованої цілі), закрито
[ADR-0092](../../../04-governance/adr/0092-backfill-marker-deferred.md)
2026-09-02 - § Маркер реконструкції не робиться. Відкритих питань до
founder-а немає. -->

## Проблема

Ціль КБЖВ живе в чотирьох скалярних полях мутабельного JSONB
(`nutrition_prefs.prefs_json` → `dailyTargetKcal` / `…Protein_g` / `…Fat_g` /
`…Carbs_g`). Запис іде per-row LWW-UPDATE-ом, який затирає попереднє значення
без сліду.

Наслідок - знахідка **E-1** аудиту
([`product-knowledge-nutrition.md`](../../audits/product-knowledge-nutrition.md)):
девʼять ретроспективних читачів беруть ПОТОЧНУ ціль і прикладають її до МИНУЛИХ
днів. Людина, що місяць їла на 2400 і сіла на сушку з 1800, відкриває дайджест
за той місяць і бачить його червоним. Історія переписалась заднім числом, хоча
жоден `Meal` не змінився.

Ця спека - **передумова** до
[`nutrition-adaptive-goal.md`](./nutrition-adaptive-goal.md), і саме тому вона
термінова. Адаптивна ціль перераховує число щотижня; поки читачі беруть
поточну ціль, разова знахідка E-1 стане систематичним автоматичним
перефарбовуванням історії щотижня, без відома людини.

## Мета

Жоден ретроспективний читач більше не бере `NutritionPrefs.dailyTarget*` для
дня в минулому. Ефективна ціль дня `D` резолвиться з append-only журналу
`nutrition_goal_periods` через `resolveEffectiveGoal`. Дні, чия ефективна ціль
має `origin='backfill'`, лишаються без окремого маркера - причина в
§ Маркер реконструкції не робиться.

Перевірна умова: користувач із ціллю 2400 у липні й 1800 у серпні відкриває
тижневий дайджест за липень і бачить липневі дні судженими за 2400. Після
зміни цілі сьогодні агрегати за попередні дні не змінюються взагалі.

## Що вже є в репо (читати перед стартом)

Це **не** greenfield. Дві з трьох стадій лежать у `main` і були зроблені саме
під цю задачу:

| Що                         | Де                                                                                                                                                                                                                          | Стан                                               |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| Append-only журнал (схема) | [`087_nutrition_goal_periods.sql`](../../../../apps/server/src/migrations/087_nutrition_goal_periods.sql)                                                                                                                   | Стадія 1: таблиця, CHECK на `origin`               |
| Шлях запису сходинок       | [`adapter.goalPeriods.ts`](../../../../apps/web/src/modules/nutrition/lib/sqliteWriter/adapter.goalPeriods.ts) + `diffGoalPeriodOps` у [`diff.ts:412`](../../../../apps/web/src/modules/nutrition/lib/sqliteWriter/diff.ts) | Стадія 1: **пишеться паралельно** з `prefs-upsert` |
| Резолвер ефективної цілі   | [`nutritionGoals.ts`](../../../../packages/nutrition-domain/src/nutritionGoals.ts)                                                                                                                                          | Стадія 2: `resolveEffectiveGoal` / `…ForRange`     |
| Pull-шлях журналу          | [`applyPullGoalPeriods.ts`](../../../../apps/web/src/core/syncEngine/applyPullGoalPeriods.ts)                                                                                                                               | Стадія 1: сходинки їдуть між пристроями            |

**Ключовий факт для оцінки обсягу:** `resolveEffectiveGoal` має в репо рівно
три згадки - власне визначення, свій тест і серверний
[`applySyncGoals.ts`](../../../../apps/server/src/modules/sync/nutrition/applySyncGoals.ts).
Ретроспективних споживачів **нуль**. Уся ця спека - підключення споживачів до
вже готового резолвера, а не будівництво механізму.

**Друга оцінка обсягу:** `dailyTargetKcal` у `apps/server/src` зустрічається
рівно у міграції 087 і її тесті. Серверної частини в цій поставці **немає** -
весь cutover живе у вебі й мобілці.

## Девʼять читачів

ADR-0091 називає їх словами; списку файлів не існувало ніде. Ось він, звірений
з `main` 2026-09-02. Кожен читач бере поточну ціль і прикладає її до дня в
минулому.

| #   | Читач                | Файл                                                                                                                                       | Що судить минулим                     |
| --- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------- |
| 1   | Тижневий дайджест    | [`core/insights/useWeeklyDigest.ts`](../../../../apps/web/src/core/insights/useWeeklyDigest.ts)                                            | сім днів тижня проти цілі             |
| 2   | Coach-снапшот        | [`core/insights/useCoachInsight.ts`](../../../../apps/web/src/core/insights/useCoachInsight.ts)                                            | вікно спостереження                   |
| 3   | Рекомендації         | [`core/lib/recommendationEngine.ts`](../../../../apps/web/src/core/lib/recommendationEngine.ts)                                            | історія для порад                     |
| 4   | Stories              | [`core/stories/components/slides/NutritionSlide.tsx`](../../../../apps/web/src/core/stories/components/slides/NutritionSlide.tsx)          | період слайду                         |
| 5   | Тижневий графік      | [`modules/nutrition/components/WeekKcalCard.tsx`](../../../../apps/web/src/modules/nutrition/components/WeekKcalCard.tsx)                  | сім стовпчиків проти лінії цілі       |
| 6   | Стрік 7 днів         | [`modules/nutrition/hooks/useStreakSevenDaysInsight.ts:73`](../../../../apps/web/src/modules/nutrition/hooks/useStreakSevenDaysInsight.ts) | сім днів назад, `ratio = kcal / goal` |
| 7   | Дайджест (mobile)    | [`core/dashboard/weeklyDigestAggregates.ts`](../../../../apps/mobile/src/core/dashboard/weeklyDigestAggregates.ts)                         | дзеркало №1                           |
| 8   | Coach-снапшот (mob.) | [`core/dashboard/coachSnapshot.ts`](../../../../apps/mobile/src/core/dashboard/coachSnapshot.ts)                                           | дзеркало №2                           |
| 9   | Графік (mobile)      | [`modules/nutrition/components/WeekKcalChart.tsx`](../../../../apps/mobile/src/modules/nutrition/components/WeekKcalChart.tsx)             | дзеркало №3                           |

**№5 і №9 приймають ціль пропом** (`targetKcal: number`), а не читають prefs
самі. Правити треба **виклик**, не компонент: у вебі це
[`NutritionDashboard.tsx:279`](../../../../apps/web/src/modules/nutrition/components/NutritionDashboard.tsx).
Компонент після цього отримує не число, а ряд «ціль на кожен день тижня» -
див. рішення D3.

## Хто в девʼятку НЕ входить (і чому)

Не чіпати. Кожен із них законно бере ПОТОЧНУ ціль:
`DailyPlanCard`, `DailyPlanGoalSelectors`, `DailyPlanWarnings`, `MealStrip`,
`NutritionDashboard` у частині «сьогодні», `core/settings/NutritionSection`,
`core/onboarding/seedDemoData/seedNutrition.ts`, `nutritionBackup.ts` (веб і
мобілка), `sqliteWriter/diff.ts`.

**Прикордонний випадок, який треба вирішити свідомо:**
[`core/lib/hubChatContext/sections.ts:190`](../../../../apps/web/src/core/lib/hubChatContext/sections.ts)
не судить минуле сам - він друкує рядок `[Харчування ціль] N ккал/день`, а
нижче окремо збирає семиденний масив калорій. Формально це поточна ціль, тобто
законне вживання. Фактично модель отримує ціль і тиждень поруч і поєднає їх
сама. Рішення: **рядок цілі лишається як є** (він справді про сьогодні), але
семиденний масив дістає власну ціль на кожен день із резолвера, і в промпті це
називається явно. Інакше HubChat стане десятим репейнтером, якого ніхто не
рахував.

**Суміжний шлях запису, який треба перевірити, а не переписувати:**
[`chatActions/crossActions/goalAndUtility.ts:54`](../../../../apps/web/src/core/lib/chatActions/crossActions/goalAndUtility.ts)
міняє ціль через чат (`persistNutritionPrefs({ …, dailyTargetKcal })`). Це шлях
ЗАПИСУ, не читання, і за стадією 1 він має породжувати сходинку журналу через
`diffGoalPeriodOps`. **Перевір це тестом до початку робіт.** Якщо сходинка не
пишеться, cutover читачів дасть діру: ціль змінилась, сходинки немає, наступні
дні судяться старою ціллю. Це блокер, і лікується він у стадії 1, не тут.

## Рішення дизайну

- **D1. Ефективна ціль резолвиться діапазоном, не поштучно.** Читачі беруть
  `resolveEffectiveGoalForRange` один раз на вікно, а не `resolveEffectiveGoal`
  у циклі по днях. Причина не в продуктивності, а в формі запиту: усі девʼять
  читачів працюють вікнами (тиждень, період спостереження), і поденний виклик
  спокушає закешувати перший результат і застосувати до решти - тобто відтворити
  ту саму помилку всередині нового API. Відкинуто: додати параметр `date` в
  наявні хуки й лишити скалярну ціль.

- **D2. Читач, що не має даних журналу, показує те саме, що сьогодні, а не
  порожнечу.** Якщо на день `D` сходинки немає взагалі (журнал молодший за
  лог), резолвер повертає `null`, і читач має відпрацювати як зараз - без
  відсотка й без вердикту, а не з нулем у знаменнику. Нуль у знаменнику дає
  `Infinity` і червоний день, тобто гіршу брехню за ту, яку лікуємо.

- **D3. `WeekKcalCard` / `WeekKcalChart` приймають ряд цілей, не число.**
  Проп `targetKcal: number` стає `targetKcalByDay: Array<number | null>` тієї ж
  довжини, що й `rows`. Лінія цілі на графіку перестає бути горизонтальною і
  стає східчастою - це видима зміна, і вона правильна: саме так виглядає місяць,
  у якому людина змінила ціль. `computeWeekKcalChart` міняє сигнатуру разом із
  компонентом.

- **D4. Ряд цілей не несе прапорця реконструкції.** Форма ряду -
  `Array<number | null>`, без `{ reconstructed: boolean }`. Причина в
  § Маркер реконструкції не робиться; якщо маркер колись знадобиться, форма
  розширюється адитивно і жоден із девʼяти читачів не переписується.

- **D5. `METRICS_VERSION` бампається один раз на всю поставку.** Не по разу на
  читача. Усі девʼять зʼїжджають в одному PR саме тому, що частковий cutover
  дав би дайджест на новій методиці й коуч на старій - два числа за той самий
  тиждень.

## Порядок і залежність від паралельної роботи

**Координація - знята 2026-09-03.** Паралельна
[`routine-flexible-weekly-frequency.md`](./routine-flexible-weekly-frequency.md)
уже бампнула `METRICS_VERSION` 11 → 12 (`3808060a4`, у `main`). Ця спека бере
**12 → 13**. Перевірка перед стартом PR-3 лишається корисною як звичка:
`grep -n "METRICS_VERSION = " packages/shared/src/lib/metricsVersion.ts` має
показати `12`; інше число означає, що хтось ще рухав межу і докстрінг треба
перечитати.

### PR-1 - доказ, що шлях запису цілий (`test(web)`) - ✅ у `main`

Приземлився PR [#1044](https://github.com/SkOrDs-02/sergeant/pull/1044)
(`8bc552390`, гілка `claude/kbju-cutover-pr1`, коміт `fab31b5fe` «довести, що
зміна цілі доходить до журналу КБЖВ»):
[`goalPeriodsEntrypoint.test.ts`](../../../../apps/web/src/modules/nutrition/lib/sqliteWriter/__tests__/goalPeriodsEntrypoint.test.ts).
Тест зелений - блокера з § «Суміжний шлях запису» немає, далі йти можна.

Поведінка не змінюється, метрика не рухається. Тест, що зміна цілі **будь-яким
шляхом** (форма налаштувань, чат-екшен `goalAndUtility`) породжує сходинку в
журналі з правильним `effectiveFrom` і `origin='manual'`. Це страховка з
§ «Суміжний шлях запису»: якщо тест червоний, стоп - далі йти не можна.

### PR-2 - ряд цілей замість числа у графіках (`feat(web)`)

`WeekKcalCard` + `computeWeekKcalChart` + виклик у `NutritionDashboard.tsx:279`
переходять на `targetKcalByDay` (D3). Мобільний `WeekKcalChart` - тим самим
патерном. Метрика поки не рухається: значення в ряду однакові, бо джерело ще
prefs. Це навмисно поведінково-нейтральний крок, який відокремлює зміну форми
даних від зміни їх джерела.

### PR-3 - cutover девʼяти читачів (`feat(web)`)

Тут вмикається поведінка й рухається метрика.

1. Усі девʼять читачів із таблиці переходять на `resolveEffectiveGoalForRange`.
2. Семиденний масив у `hubChatContext/sections.ts` - теж (див. § Прикордонний
   випадок).
3. `METRICS_VERSION` 12 → 13 плюс абзац у докстрінгу
   [`metricsVersion.ts`](../../../../packages/shared/src/lib/metricsVersion.ts)
   за домашнім стилем: що змінилось, у який бік рухається число і чому тренд
   через межу читати не можна. Тут число рухається **в обидва боки** - у кого
   ціль зростала, минуле позеленіє, у кого падала, почервоніє менше.

## Стан виконання (звірено з `origin/main` 2026-09-03)

| Крок                         | Стан         | Доказ                                                                                                                                                                                                                                                                           |
| ---------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PR-1 доказ шляху запису      | **зроблено** | [#1044](https://github.com/SkOrDs-02/sergeant/pull/1044), `goalPeriodsEntrypoint.test.ts` (131 рядок, єдиний файл PR)                                                                                                                                                           |
| PR-2 ряд цілей у графіках    | не зроблено  | `targetKcalByDay` у `apps/web/src` і `apps/mobile/src` - 0 згадок; `WeekKcalCard.tsx` / `WeekKcalChart.tsx` досі приймають число                                                                                                                                                |
| PR-3 cutover девʼяти читачів | не зроблено  | `useWeeklyDigest.ts`, `useCoachInsight.ts`, `useStreakSevenDaysInsight.ts`, `weeklyDigestAggregates.ts` досі читають `dailyTargetKcal`; `resolveEffectiveGoalForRange` поза `nutrition-domain` згадується лише коментарем у `NutritionDashboard.tsx:79`; `METRICS_VERSION = 12` |

Наступний крок для виконавця - PR-2 за § Порядок; передумови всі виконані.

## Маркер реконструкції не робиться

ADR-0091 ухвалив, що дні з `origin='backfill'` мають нести видимий маркер
«ціль відновлена». **У цій поставці він не робиться** —
[ADR-0092](../../../04-governance/adr/0092-backfill-marker-deferred.md),
рішення founder-а 2026-09-02, ухвалене на такому факті:

Бекфіл-рядок міграція 087 створює лише тим, хто логував їжу **до її
накатування**. Міграція приїхала 2026-07-25
([`#452`](https://github.com/SkOrDs-02/sergeant/pull/452)), бета-форми
опубліковані 2026-08-01, тобто журнал існував раніше за першого тестера. Усі
зміни цілі, зроблені тестерами, йдуть звичайним шляхом запису з
`origin='manual'`. Єдиний акаунт, який теоретично має бекфіл-рядок, - тестовий
акаунт founder-а, і його історію founder знає без підказки інтерфейсу.

Тобто маркер - це UI, дизайн і українська копія для популяції з нуля людей.

**Що лишається (безкоштовно, бо вже є):** значення `'backfill'` у CHECK-
констрейнті міграції 087 і в типах резолвера. Інформація не втрачається, її
просто ніхто не малює.

**Умова повернення:** якщо в проді зʼявиться акаунт із логами їжі, датованими
раніше за його першу сходинку журналу з `origin != 'backfill'`. Практично це
означає імпорт історії з іншого трекера - тобто маркер повертається разом із
[`import-external-trackers`](./import-external-trackers.md), а не сам собою.

## Поза скоупом

- **Стадія 4-5** (депрекація `NutritionPrefs.dailyTarget*` як поля) - окрема
  задача. Поточна ціль лишається там і після цієї поставки.
- **Запис сходинок з `origin='tdee'`** - це
  [`nutrition-adaptive-goal.md`](./nutrition-adaptive-goal.md), не тут.
- **Сервер** - у ньому `dailyTarget*` немає (§ Що вже є в репо).
- **Ретро-редагування журналу** («я насправді був на 2000 у липні») - не робимо
  взагалі: append-only журнал, який можна переписати, не є append-only.

## Верифікація (обовʼязково)

1. `pnpm check`.
2. **Тест на незмінність минулого** (машинна перевірка самого рішення, вимога
   `Compliance` ADR-0091): додати сходинку «сьогодні» і переконатись, що
   агрегати за попередні дні не змінились жодним числом.
3. **Грep-перевірка рецензента** (та сама вимога ADR): у діффі жоден зі
   змінених читачів не бере `dailyTarget*` для дня в минулому.
4. **Click-through:** постав ціль 2400, залогуй день, зміни ціль на 1800,
   відкрий тижневий графік - вчорашній день лишається судженим за 2400, лінія
   цілі східчаста.
5. Скріншот графіка зі східчастою лінією - у PR.

## Ризики

- **Одноразовий стрибок цифр на cutover** - неминучий і ратифікований ADR-0091
  як ціна рішення. Мітигація - бамп `METRICS_VERSION` (D5), який робить розрив
  тренду явним, а не мовчазним.
- **Журнал молодший за лог.** У ранніх користувачів сходинок за старі дні може
  не бути навіть після backfill міграції 087. Мітигація - D2.
- **Мобілка їде окремою поставкою** (EAS-лаг, зафіксовано в ADR-0091). Два
  тижні веб і мобілка показуватимуть різні числа за ті самі дні. Це прийнятно
  і мало б бути названо в релізних нотатках.

## Звʼязки

- [ADR-0091](../../../04-governance/adr/0091-goal-history-not-repainted.md) - продуктове рішення, яке ця спека виконує
- [ADR-0079](../../../04-governance/adr/0079-frozen-past-and-canonical-denominator.md) - той самий принцип для звичок
- [`nutrition-adaptive-goal.md`](./nutrition-adaptive-goal.md) - споживач цієї поставки
- Owner-скіли за routing-таблицею AGENTS.md: `sergeant-module-nutrition` + `sergeant-web-ui`
