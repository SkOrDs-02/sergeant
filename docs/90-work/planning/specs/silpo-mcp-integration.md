# SPEC: Інтеграція Silpo MCP — чеки, комора, продуктові дані

> **Last validated:** 2026-07-31 by @claude. **Next review:** 2026-10-29.
> **Status:** Scaffolded — брейншторм + спека; треки G/H і подача на хакатон чекають рішень founder-а (§ Founder-гейти)

## Контекст

Сільпо (Fozzy Group) відкрили офіційний публічний MCP-сервер: endpoint
`https://mcp.silpo.ua/mcp` (streamable HTTP), авторизація OAuth 2.1 + PKCE з
Dynamic Client Registration (`/register`, `authorization_code` + `refresh_token`
— метадані на `/.well-known/oauth-authorization-server` перевірені 2026-07-31).
39 tools: пошук/картки товарів (`silpo_get_product_details`,
`silpo_find_products_batch` до 30 паралельно), кошик, слоти доставки,
**історія покупок** (`silpo_get_my_online_orders`, `silpo_get_my_offline_orders`
— чеки з фізичних магазинів), профіль (`silpo_get_my_food_restrictions`,
`silpo_get_my_family`), лояльність (купони, персональні акції, «Власний
Рахунок»). **Checkout-tool відсутній свідомо** — агент може лише підготувати
кошик, покупку підтверджує людина в каналах Сільпо.

Довкола MCP іде хакатон «AI Factory» (реєстрація до 2026-08-31, розробка
01–14.09, фінал 30.09, фонд 100 000 ₴). Хакатон — привід, але не мета: цінність
для Sergeant — **чекові дані та український продуктовий каталог**, які замикають
петлі finyk і nutrition незалежно від результату хакатону.

## Проблема

1. **Супермаркетний чек — чорна скриня finyk.** Mono-webhook дає одну
   транзакцію «Сільпо −847.50» (MCC 5411): їжа, побутова хімія й алкоголь
   зліплені в одну категорію `food`. Це найбільша категорія витрат і
   найгірше деталізована; сутність `TxSplit` у домені є, але заповнюється
   лише руками. Це прямо в'яжеться з боргом finyk §11г (два механізми
   категоризації без цільового).
2. **Цикл nutrition «план→їжа→факт» розірваний на вході.** Комора має ціль
   «складський облік» ([nutrition §9]) — авто-списання вже працює, а
   **поповнення** робиться руками, low-stock-сигналу немає. Факт покупки
   (що реально принесли додому) в продукт не потрапляє ніяк.
3. **Укр-продукти погано покриті базами.** OFF/USDA на демо-ключах — блокер
   запуску (nutrition §6, брейншторм 1.4: «чомусь у деяких апок норм працюють
   укр продукти»). Каталог Сільпо — найбільший структурований каталог
   українського продуктового рітейлу з штрихкодами.
4. **Ідентичність продукту.** «Бачить усе життя разом» (product-overview §1):
   чек — єдиний артефакт, що водночас годує finyk (гроші) і nutrition (їжа),
   тобто робить крос-модульні зв'язки обчислюваними на рівні конкретних
   продуктів, а не агрегатів.

## Мета

Користувач з акаунтом Сільпо зв'язує його з Sergeant один раз; після цього
(а) супермаркетні транзакції у finyk розгортаються в чек з позиціями і
пропонують спліт за реальними категоріями, (б) куплені продукти пропонуються
до поповнення комори, (в) укр-продукти знаходяться у пошуку їжі з КБЖВ.
Все без жодного нового ручного вводу і без створення дублікатів витрат.

## Брейншторм: каталог фіч-кандидатів

| #   | Фіча                                                                                              | Модуль    | Цінність | Складність | Канон-конфлікт                          |
| --- | ------------------------------------------------------------------------------------------------- | --------- | -------- | ---------- | --------------------------------------- |
| A   | **Silpo Connect** — OAuth-зв'язка в налаштуваннях, токени server-side                             | core      | інфра    | M          | ні                                      |
| B   | **Чекове збагачення finyk** — item-рівень існуючих mono-транзакцій + пропозиція авто-спліту       | finyk     | ★★★      | M          | ні (оверлей, не нова витрата)           |
| C   | **Авто-поповнення комори** — куплене → `pantry_events: replenish` + low-stock петля               | nutrition | ★★★      | M          | ні (закриває ціль §9)                   |
| D   | **Укр-продуктові дані** — Silpo product details → foodDb (КБЖВ, штрихкоди) як 4-те джерело        | nutrition | ★★☆      | S–M        | ні (якщо MCP реально віддає КБЖВ — §0)  |
| E   | **«З покупок» швидкий лог їжі** — куплені продукти як підказки у FoodPicker                       | nutrition | ★☆☆      | S          | ні                                      |
| F   | **Item-рівень кореляції** — категорії чеків як нові пари для digest-кореляцій (детерміновано)     | hub-coach | ★★☆      | M          | пороги §6.2 обов'язкові                 |
| G   | **Shopping list → кошик Сільпо** — HubChat-агент наповнює кошик, людина завершує в апці Сільпо    | hub-coach | ★★☆      | M–L        | **ГЕЙТ**: nutrition §8.2                |
| H   | **Тижневий агент закупівлі** — план тижня + бюджет + обмеження → готовий кошик (Pro)              | hub-coach | ★★★?     | L          | **ГЕЙТ**: nutrition §8.2 + finyk §7     |
| I   | ~~Price-tracker / промо-хантер~~ — потребує власних щоденних снапшотів цін, окремий продукт       | —         | —        | —          | свідомо не робимо у v1                  |
| J   | ~~Автономні покупки / push «купуй зараз»~~                                                        | —         | —        | —          | checkout відсутній + проактивність = Pro-майбутнє |

Рекомендований v1: **A + B** (finyk-трек), далі **C**, потім **D/E**. F —
після накопичення даних. G/H — окремою фазою лише після founder-рішення.

## Founder-гейти (продуктові рішення, що блокують частину треків)

1. **nutrition §8.2 [ІНТЕРВ'Ю]: «Не замовлення й не доставка їжі».** Треки
   G/H готують кошик у Сільпо (checkout у MCP відсутній — фінальний клік
   завжди за людиною в каналах Сільпо). Формально це не замовлення, але дух
   межі — питання founder-а. До рішення G/H не реалізуються. Треки A–F межі
   не торкаються: це читання даних, а не дія в рітейлі.
2. **finyk §7: «Жодних платежів і переказів».** Кошик — не платіж, але якщо
   G/H ратифікуються, їх дім — hub-coach шар (HubChat tool з confirm-гейтом),
   не finyk.
3. **Оферта Сільпо.** Прочитати публічну оферту MCP: чи дозволене використання
   поза хакатоном у комерційному продукті; чи є обмеження на зберігання
   даних. Без цього — тільки спайк на власному акаунті founder-а.
4. **Приватність чекового потоку** (finyk §6.4 — відкрите). Чекові позиції —
   найдетальніші споживчі дані в продукті. Рішення: item-нейми потрапляють у
   LLM-контекст лише через існуючий конверт `wrapAndScanToolResults` +
   маскування (конституція §9.9 «анонімізація перед LLM» — частково
   реалізована); у телеметрію — ніколи.
5. **Хакатон-трек.** Чи подаємось (реєстрація до 31.08)? Якщо так — Phase 0+1
   цієї спеки і є демо («агент, що пояснює твій чек»), але хакатонний demo
   краще зібрати як тонкий standalone поверх тих самих `/api/silpo/*`
   ендпоінтів, не тягнучи Sergeant-акаунт у журі.

## Рішення дизайну

- **Server-side MCP-клієнт, клієнтська модель tools незмінна.** У HubChat
  «сервер визначає інструменти — клієнт їх виконує» (hub-coach §2); server-side
  executor-а не існує, і ця інтеграція його НЕ вводить. Silpo-виклики живуть
  за нашими REST-ендпоінтами `/api/silpo/*`; майбутні HubChat-tools (якщо
  будуть) — звичайні client-executed tools, що фетчать наш REST. Відкинуто:
  server-side tool dispatch у `chat.ts` — інвазивна зміна контракту з web.
- **Мінімальний власний MCP-клієнт без нових залежностей.** MCP поверх
  streamable HTTP — це JSON-RPC POST (`initialize` → `tools/call`); нам треба
  5–7 tools, не весь протокол. Відповідає преференсу «нуль нових залежностей»
  (`docs/00-start/agents/decisions.md`, 2026-06-08). Відкинуто:
  `@modelcontextprotocol/sdk` — тягне транспорти/схеми, які нам не потрібні;
  повернутись до SDK, якщо протокольна поверхня розростеться.
- **OAuth за mono-патерном, не через Better Auth social.** Це авторизація до
  стороннього API, не login. `GET /api/silpo/connect` → redirect на
  `auth.silpo.ua` (PKCE S256) → `GET /api/silpo/callback` → обмін коду,
  AES-256-GCM шифрування токенів у `silpo_connection` (дзеркало
  `mono_connection` + `token_key_version` з key ring). DCR — одноразово,
  `client_id` у env. Refresh-flow — lazy при 401, за зразком
  `mono/tokenStore.ts` (opportunistic re-encrypt теж копіюємо).
- **Чеки зберігаємо у себе, назавжди.** MCP може змінитись чи закритись після
  хакатону — снапшот чеків у нашому PG робить фічу незалежною від долі
  платформи. Суми — **копійки як integer/bigint** (як `mono_transaction`),
  день — Europe/Kyiv. Сирий payload — у `raw JSONB`.
- **Збагачення, а не створення витрат.** Гроші за Сільпо-покупки вже входять
  через Mono-webhook. Чек мапиться на існуючу `mono_transaction`
  (детермінований matcher у `packages/finyk-domain`: точна сума в копійках +
  дата ±1 доба Kyiv + MCC 5411/опис; `mono_transaction.receipt_id` — сильний
  сигнал, звірити формат на спайку §0). Незматчений чек (готівка) → пропозиція
  створити manual expense, ніколи мовчки. Нуль ризику подвійного обліку.
- **Спліт — пропозиція, не мовчазний запис.** Оверлеї = ручний світ у сенсі
  durability (finyk §5): у деталях транзакції з'являється «Розбити за чеком»
  (категорії item-ів → `TxSplit[]` → існуюча `finyk_tx_splits`), користувач
  підтверджує одним тапом. Категорії item-ів — детермінований мапер
  «дерево категорій Сільпо → finyk-категорії + FOOD_CATEGORIES»; AI — лише
  прискорювач для «інше» (nutrition §7-логіка), через існуючий
  enrichment-патерн.
- **Комора — через готовий ledger.** Куплені позиції → екран підтвердження →
  `nutrition_pantry_events` kind `replenish` (схема вже існує, міграція 086;
  мапінг name→pantry item через `norm`-нормалізацію foodDb; нерозпізнане —
  пропозиція нової позиції). Low-stock додається як наслідок: у комори
  з'являється цикл поповнення, з якого видно rate споживання.
- **Продуктові дані — четверте джерело каскаду.** Якщо спайк підтвердить КБЖВ
  у `silpo_get_product_details`: сервер-проксі у нинішній каскад
  (`OFF → USDA → Silpo` для uk-запитів; штрихкоди — аналогічно) з
  `recordExternalHttp` + circuit breaker за плейбуком
  `onboard-external-api.md`. Якщо КБЖВ немає — трек D звужується до
  назв/штрихкодів/цін.
- **Sync у клієнт — read-only через API, не через op-log.** Чеки народжуються
  на сервері і не редагуються клієнтом — це mono-модель (React Query поверх
  REST), не dual-write. `OP_LOG_TABLE_REGISTRY` не чіпаємо. RQ-ключі — нова
  фабрика `silpoKeys` у `queryKeys.ts` (Hard Rule #2).
- **Free/Pro.** Зв'язка + чеки + спліти + комора = **Free** (аналогія
  «банк-sync безкоштовний назавжди», брейншторм 3.3: імпорт власних даних —
  core value, не AI). Агентні треки G/H, якщо ратифікуються — Pro.
- **Ізоляція збою.** Silpo-недоступність не деградує нічого існуючого: банер
  staleness за зразком `MonoStalenessBanner` (finyk §6.3), rate-limit 429 →
  експоненційний backoff, полінг on-demand (кнопка «Оновити чеки» + при
  відкритті finyk, TTL-кеш) — webhook-ів у Сільпо немає; щоденний фоновий
  job (BullMQ вже в deps) — пізніше, опційно.

## Поверхня змін (v1 = треки A+B)

- `apps/server/src/migrations/091_silpo_integration.sql` (+ `.down.sql`) —
  `silpo_connection` (дзеркало mono), `silpo_receipts`
  (PK `(user_id, receipt_id)`, `purchased_at`, `store_id`, `channel
  online|offline`, `total_kop BIGINT`, `raw JSONB`), `silpo_receipt_items`
  (позиції: name, qty, unit, `price_kop`, category_slug, barcode?),
  `finyk_tx_receipt_links` (PK `(user_id, transaction_id)` → receipt_id) —
  link-патерн як `finyk_mono_debt_links`. Hard Rule #4.
- `packages/db-schema/src/pg/silpo.ts` + barrel + snapshot-тест.
- `apps/server/src/modules/silpo/` — `mcpClient.ts` (JSON-RPC, timeouts,
  backoff, `recordExternalHttp("silpo", …)`), `oauth.ts` (PKCE + DCR),
  `tokenStore.ts` + reuse `mono/crypto.ts`→спільний `lib/`, `receipts.ts`
  (pull + upsert), `matcher.ts` (виклик доменного matcher-а), serializers
  (bigint→number, Hard Rule #1).
- `apps/server/src/routes/silpo.ts` — `/api/silpo/connect|callback|disconnect|
  sync-state|receipts|receipts/:id` + реєстрація в `routes/index.ts`;
  rate-limit; `requireSession`.
- `apps/server/src/env/env.ts` — `SILPO_MCP_URL`, `SILPO_OAUTH_CLIENT_ID`,
  `SILPO_TOKEN_ENC_KEY(S)` за парним патерном `MONO_*`; документація
  `env-vars.md`.
- `packages/shared/src/schemas/silpo.ts` + `packages/api-client` endpoints +
  contract-тест (Hard Rule #3, `pnpm api:generate-openapi*`).
- `packages/shared/src/lib/pii.ts` — `silpoToken`, loyalty-поля у
  `REDACT_KEY_NAMES`; Hard Rule #21 (`redactPaths` за потреби).
- `packages/finyk-domain/src/domain/receiptMatching.ts` — чистий matcher +
  тести; мапер категорій.
- `apps/web`: `core/settings` — картка інтеграції Сільпо (за зразком Mono);
  `modules/finyk` — секція «Чек» у деталях транзакції + CTA спліту;
  `shared/lib/api/queryKeys.ts` — `silpoKeys`. 44px touch targets, UA-копія
  за style-guide.
- Owner-скіли: `sergeant-server-api` (основна поверхня) →
  `sergeant-data-and-migrations` (091) → `sergeant-web-ui`;
  PR-boundary — `sergeant-review-and-merge`.
- Канони: якщо/коли треки стають продуктовою поведінкою — оновлення
  `finyk.md` (§4 сутності: чек-оверлей) і `nutrition.md` (§9 комора) **у тому
  ж PR** (Hard Rule #15 + правило канонів).

## Фази

0. **Спайк (≤1 день, до будь-якого коду в main).** Власний акаунт founder-а,
   Claude Desktop / скрипт: пройти OAuth, смикнути `get_my_offline_orders`,
   `get_my_online_orders`, `get_product_details`. Верифікувати: глибина
   історії; чи є в чеках item-рівень (назви/кількості/ціни) чи лише суми;
   чи є КБЖВ і штрихкоди в картці товару; формат `receipt_id` vs
   `mono_transaction.receipt_id`; фактичні rate limits. **Вихід: go/no-go
   по треках B (item-рівень) і D (КБЖВ).** Якщо offline-чеки без позицій —
   B звужується до online-замовлень, пріоритет переходить C/G-рішенню.
1. **Треки A+B** — connect + чеки + збагачення finyk (поверхня вище).
2. **Трек C** — поповнення комори + low-stock.
3. **Треки D/E** — продуктові дані + «з покупок».
4. **[за гейтом §1/§2] G/H** — HubChat-агент кошика (deferred tools, confirm
   перед записом у зовнішній кошик, Pro).

## Поза скоупом v1

- Треки G/H (founder-гейт), price-tracker (I), будь-яка автономна покупка (J).
- Фоновий щоденний sync (BullMQ job) — після того, як on-demand доведе
  цінність; проактивні push-и — ніколи в Free (hub-coach §9).
- HubChat-tools по чеках — не в Phase 1 (tool-бюджет уже 77 tools / ~43 KB;
  нові tools лише deferred і лише після того, як REST-поверхня устаканиться).
- Мобільний паритет (web = reference, finyk §9).
- Інші рітейлери (АТБ, Novus) — адаптерна межа `modules/silpo/mcpClient.ts`
  свідомо тонка, але узагальнення інтерфейсу — тільки при другому рітейлері
  (ініціатива 0022 — парасолька для цього).

## Верифікація (обов'язково)

1. `pnpm check` зелений; contract-тести silpo-схем; migration-тест 091
   (Testcontainers) + `rollback-sanity`.
2. Click-through: Налаштування → «Зв'язати Сільпо» → OAuth-редірект →
   повернення зі статусом connected → «Оновити чеки» → у finyk-транзакції
   «Сільпо» з'являється секція «Чек» з позиціями → «Розбити за чеком» →
   спліт видно в аналітиці категорій.
3. Негативні шляхи: відкликання доступу (401 → refresh → повторний 401 →
   статус `reauth_required` + банер), 429 (backoff, без падіння запиту
   користувача), чек без mono-пари (пропозиція manual expense, нічого не
   створено мовчки).
4. Тести matcher-а: точна сума/дата, зсув доби через Kyiv-межу, два чеки на
   одну суму в один день (амбігуїті → не матчимо, лишаємо ручний вибір).

## Ризики та відкриті питання

- **Item-рівень offline-чеків не гарантований** — найбільший ризик цінності;
  знімається спайком §0 до старту Phase 1.
- **Оферта/ToS MCP** — комерційне використання і зберігання даних поза
  хакатоном; знімає founder (гейт §3).
- **Стабільність платформи після 30.09** — мітигація: власні снапшоти чеків,
  тонкий адаптер, жодна core-фіча не залежить від live-Silpo.
- **Rate limits per-user невідомі** — виміряти на спайку; TTL-кеш + backoff
  у дизайні з першого дня.
- **Мапінг «назва позиції → продукт/комора» нечіткий** («Молоко Яготинське
  2.6% 900г») — v1: `norm`-нормалізація + підтвердження користувачем; AI-мапер
  — прискорювач пізніше, поза критичним шляхом.
- **Приватність чекового потоку** — формулювання обіцянки (finyk §6.4
  відкрите) має відбутись до відкриття фічі за межі founder-акаунта.
