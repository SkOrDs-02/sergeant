# Харчування: що рахує код, а що вгадує модель

> **Last touched:** 2026-08-29 by @claude. **Next review:** 2026-12-01.
> **Status:** Scaffolded
> **Маршрут:** /yizha · **Питання людини:** Як рахувати їжу, коли половини мого холодильника немає в жодній базі

## Мета сторінки

Сторінка для людини, яка вже пробувала рахувати КБЖВ і кинула на пошуку: у міжнародних базах немає ні кабачка з вагою від касового терміналу, ні локального бренду сиру. Вона має зрозуміти дві речі: ядро модуля працює руками й без AI, а кожна цифра в журналі несе походження, тож вгадане видно окремо від виміряного. Сторінка не продає фото-лог і не обіцяє дієтології – вона показує механіку і називає межі.

## Копія

> **Технічні нотатки для перенесення в JSX.** Розряди чисел – нерозривним пробілом (7 576, 2 054, 6 395). Апостроф – `ʼ` (U+02BC). Тире – тільки `–` (U+2013). Знак `≈` у тексті – звичайний символ, не картинка. Дата оновлення в хедері сторінки ставиться така сама, як `lastmod` у `routeMeta.json`.

### Герой (hero)

Надзаголовок (eyebrow):

    Модуль · Харчування

H1:

    Харчування: що рахує код, а що вгадує модель

Підпис під H1:

    Оновлено 29.08.2026 · автор Sergeant

Лід:

    Підрахунок їжі ламається не на дисципліні, а на пошуку. Третина полиці має лише внутрішньомагазинний ваговий код, якого в жодній глобальній базі немає, а те, що має справжній штрихкод, часто лежить у базі без білків і жирів. Тут я показую, звідки модуль бере числа, у яких випадках він каже «приблизно», і де просто зупиняється.

### Коротка відповідь (short-answer)

Блок «Коротка відповідь» – та сама форма, що на гайдах: темна картка, текст одним абзацом.

    Ядро модуля ручне: пошук, штрихкод, збережені прийоми, копіювання вчорашнього дня. AI сюди лише додає швидкості, і без нього нічого не ламається. Перед зовнішніми джерелами стоять дві власні бази: 7 576 товарів української полиці, у 2 054 з них повне КБЖВ, і 390 базових продуктів у 19 категоріях для того, що продається на вагу. Те, що оцінило фото, підписано окремо: якщо більшість калорій дня прийшла з фото, поруч із сумою стоїть знак ≈.

### Ручне ядро (core-manual)

H2:

    Усе головне працює без AI

Тіло:

    Прибери AI – і лишиться повний робочий трекер, а не аварія. Штрихкод шукає продукт у продуктових базах, а не в мовній моделі. Збережений прийом повторюється тапом. Учорашній день копіюється цілком.

    Додавання прийому починається з вибору джерела, і джерел чотири вкладки: Пошук, Скан, Фото, Своє. Порядок за частотою, а не за складністю: більшість прийомів додають пошуком або повтором, а «Своє» стоїть останнім, бо це шлях, коли решта не спрацювала.

Підблок «Два ручні режими» – таблиця з двох рядків:

    | Що в тебе на руках                | Режим         | Що вводиш                        |
    | --------------------------------- | ------------- | -------------------------------- |
    | Упаковка з таблицею на 100 г      | З упаковки    | КБЖВ на 100 г і скільки зʼїв     |
    | Готова тарілка без жодних цифр    | Готова страва | КБЖВ за всю порцію               |

Підпис під таблицею:

    Це рознесено навмисно. Раніше ручний ввід був однією формою з полями «Ккал / Білки / Жири / Вуглеводи», а людина з упаковкою в руках читала етикетку, тобто значення на 100 г, і вводила їх туди, де очікувалась ціла порція. Перемикач одиниці нічого не конвертує сам: він лише вирішує, який ввід показати. Тиха конвертація була б рівно тією помилкою, проти якої цей екран і переробляли.

Абзац про сканер:

    Сканер відкривається сам, щойно ти обрав вкладку «Скан»: якщо вхід у розділ уже означає намір, зайвий тап на підтвердження цього наміру не потрібен. Якщо код помʼятий і не зчитується, через 15 секунд сканер каже, чому так буває, і дає кнопку «Ввести вручну».

Абзац про комору:

    Якщо прийом додано з позиції комори, комора списує її сама: спожиті грами перераховуються в одиницю позиції – грами й кілограми один до одного, мілілітри через щільність, штуки через вагу штуки. Позиція комори – це картка продукту, а не рядок чека: зверху одне «Молоко · 2 л», усередині – з чого воно складається. Жирність не втрачається, бо молоко 2,6% і молоко 1% відрізняються калорійністю вдвічі, і звести їх в один рядок означало б збрехати про калорії.

### Дві бази (databases)

H2:

    Дві бази, бо одної не вистачає

Тіло:

    Перша база – каталог товарів: 7 576 позицій українського зрізу Open Food Facts, з них 6 395 зі штрихкодом на префіксі 482 і 2 054 з повним КБЖВ. Каталог стоїть перед зовнішніми джерелами не заради швидкості, а заради квот: без нього ліміт витрачався б на кожен скан, з ним – лише на кожен новий товар. Хіт від зовнішнього джерела дописується назад, тож база росте з реальних сканів.

    Каталог не віддає рядки, у яких калорійність не сходиться з макросами за Атвотером. Такі рядки лишаються в таблиці для розбору, але видати їх означало б покласти завідомо хибне число в чийсь день. Реальні приклади з посіву: кола з 606 ккал на 100 мл і пластівці з нулем калорій при 447 за макросами.

    Друга база – 390 базових продуктів у 19 категоріях: огірок, кабачок, сир на вагу. Найбільші категорії – овочі й гриби (46), молочні (44), фрукти й ягоди (39), мʼясо і птиця (38). Сорок позицій мають куровані синоніми, тож «помідор» знаходить те саме, що «томат». Числа тут звіряються з макросами за Атвотером ще до коміту, тож друкарська помилка в цифрі не доїжджає до чийогось дня.

Підпис (важлива чесність про перше число):

    7 576 – це розмір каталогу, а не кількість товарів із повним КБЖВ. Повне КБЖВ мають 2 054 з них.

### Чому потрібна друга база (weight-codes)

H2:

    Третину полиці штрихкод не закриває в принципі

Тіло:

    Вимір на реальній полиці: 35 зі 104 позицій мають внутрішньомагазинний ваговий код. Такий код генерує касовий термінал конкретної мережі, тож у глобальних базах його немає й ніколи не буде. Скільки б не ріс каталог товарів, ця третина лишиться поза ним назавжди.

    Тому базовий продукт шукається назвою, а не кодом. Огірок з ринку – це «огірок», а не спроба вгадати ферму. Одне джерело правди живить обидві поверхні: сервер засіває ним свою таблицю, застосунок – локальну базу для офлайну. Дві окремі копії розійшлись би першою ж правкою, і той самий продукт мав би різні числа онлайн і офлайн.

### Вгадане не видається за виміряне (estimated)

H2:

    Знак ≈ там, де більшість калорій дня з фото

Тіло:

    Кожна цифра КБЖВ у журналі несе походження: база, етикетка, ручний ввід або фото. Денне кільце рахує, яка частка калорій дня прийшла з фото-оцінки – саме за калоріями, а не за кількістю прийомів. Три ручні прийоми по 100 ккал плюс одна фотка на 900 ккал – це 75% вгаданого дня, а не 25%.

    Понад 50% кільце показує ≈ перед сумою і підпис, що більшість калорій сьогодні – з фото-оцінки. Той самий підпис іде текстом у `aria-label`, бо знак не може бути єдиним сигналом. Смуга дня рахує ту саму частку за тією самою логікою, а мінімум показаної частки – 1%: округлення до нуля стверджувало б «усе з бази», хоч здогадка в дні є.

### Неповний день не читається як «мало зʼїв» (incomplete-day)

H2:

    Пропущений обід – це відсутні дані, а не дефіцит

Тіло:

    Людина, яка забула залогувати обід, не недоїла. Тому день, у якому менше трьох записаних прийомів, малюється пунктирним треком, а не приглушеним кольором, і текстовий еквівалент іде в `aria-label`.

    У тижневому графіку день без записів несе плаский трек, а не стовпчик: раніше він малювався огризком у три пікселі, тобто виглядав так само, як день на 50 ккал. Тап по такому дню каже «немає записів», а не «0 ккал». Середнє під графіком ділиться на дні із записами, а не на сім.

    Шкала графіка завжди має названу опору. З ціллю це пунктирна лінія цілі й підпис «ціль N», без цілі – підпис «макс N». Доти стелею був максимум самого тижня, тож найвищий стовпчик виходив на сто відсотків завжди – і при 800 ккал, і при 3 000.

### Оцінка має право відмовитись (not-food)

H2:

    «Не бачу тут страви»

Тіло:

    На початку серпня 2026 фото кота ще поверталось у застосунок як страва: назва «Кіт», впевненість 100%, нулі в усіх чотирьох числах – і кнопка «Зберегти в журнал» під цим усім. Не-їжа потрапляла в денний підсумок нарівні з обідом.

    Тепер відповідь несе окреме поле «це їжа», і межу тримає код, а не слухняність моделі: відмову виводить і явне поле, і відсутність будь-якого додатного КБЖВ у моделі, яка це поле проігнорувала. На відмові екран каже «Не бачу тут страви» і не пропонує ні збереження, ні уточнення порції.

    Відмова ще й знає, кого відшила. Категорій рівно три – тварина, людина, решта – бо рівно три різні репліки: тваринку пропоную погладити, людині – навести камеру на тарілку, для решти лишається нейтральний текст. На фото кота модель спрацювала бездоганно: вона впізнала кота, і відповідати на це «спробуй інше фото» тим самим текстом, що й на розмитому кадрі, було б неправдою.

    Окремо – нулі. Нуль у калоріях більше не означає «не знаю»: поки в оцінці лишаються невідповіджені питання, плитки показують прочерк замість «0». Склянка води і чай без цукру – легальний нуль, і затирати його було б брехнею в інший бік.

### Межі (boundaries)

H2:

    Чого модуль не робить

Список:

    – Не дієтологія й не медицина. Жодних діагнозів, лікувальних дієт і гарантій щодо алергенів: заборона вшита в промпти порад, а не тримається на тоні.
    – Не замовлення й не оплата. Автономних покупок не буде: оформлення, оплата й доставка лишаються за тобою в каналах магазину.
    – Промах штрихкоду поки нікуди не записується. Якщо товару немає в базах, ручна форма не збереже його код, тож завтра той самий скан дасть той самий промах. Це відсутня ланка, і саме вона обмежує покриття сильніше за розмір каталогу.
    – Комора не нагадує, що продукт закінчується. Списання працює, сигналу про вичерпання поки немає.
    – Ціль КБЖВ – орієнтир, а не вирок. Модуль показує залишок нейтрально і не має права викликати провину за їжу.
    – Працює в браузері: на телефоні й компʼютері. Мобільний застосунок у розробці.

Мікрокопія під списком:

    Куди їдуть фото страв і що взагалі бачить Sergeant – окремою сторінкою: «Твої дані» (`/data`). Перше фото завжди чекає явного «Зрозуміло»: перевірка кадру відбувається до відправлення.

### Скріншот модуля (screenshot)

Одне зображення, `/screens/nutrition.webp`.

alt:

    Екран Харчування: кільце 1250 із 2 200 ккал, білки, жири й вуглеводи, тижневий графік і вода за день

Підпис (figcaption):

    Екран бети. Дані на ньому з демо-режиму продукту

### Гайди про їжу (guides)

H2:

    Далі про їжу

Два лінки з описом у один рядок:

    «Як рахувати КБЖВ, коли в базі немає українських продуктів» (`/guides/kbzhv`) – пʼять типових ситуацій і що робити з кожною.

    «Калорії з фото: чи можна порахувати і як точно» (`/guides/foto-kalorii`) – що фото впізнає, чого воно не бачить і коли потрібні ваги.

Хвіст сторінки (та сама форма, що на гайдах: текст плюс один лінк):

    Їжа – один із чотирьох модулів. Як вони зводяться разом і чому Sergeant мовчить, коли звʼязку немає: «Звʼязки» (`/zvyazky`). Що працює сьогодні, а що ні: «Доповідь про стан» (`/stan`).

    Стати в чергу → `/beta`

## title і description

Рядок для `apps/landing/src/lib/routeMeta.json`:

Блок нижче – валідний JSON-обʼєкт; решта маршрутів у файлі лишається як є.

```json
{
  "/yizha": {
    "title": "Підрахунок КБЖВ з українськими продуктами",
    "description": "Ядро модуля Харчування працює без AI. Перед зовнішніми джерелами стоять дві власні бази: 7 576 товарів української полиці і 390 базових продуктів.",
    "lastmod": "2026-08-29",
    "ogImage": "/og/yizha.png"
  }
}
```

JSON-LD за схемою гайдів: `Article` + `author: Person` + `publisher: Organization`, `inLanguage: "uk"`, `dateModified` = `lastmod`.

## Що ця сторінка НЕ обіцяє

- **Фото-лог як перевагу.** Фото страви описане лише як механіка з відмовою і позначкою оцінки. Сканерів калорій з фото на ринку багато, і аргумент «саме тому Sergeant» на цьому не будується.
- **Генерацію планів харчування.** Код генерує план на день і на тиждень, але founder називає тренерські плани харчування межею модуля. Суперечність не знята, тож фіча на сторінці не рекламується взагалі.
- **Тарифи, квоти і платні плани.** Комерція вимкнена прапорцем, тож ціни, ліміти й слово «Pro» на сторінці не звучать. Фото-аналіз описано без згадки, на якому плані він доступний (див. openQuestions).
- **Чеки Сільпо як робоче джерело продуктів.** Інтеграція за вимкненим прапорцем (`SILPO_ENABLED` за замовчуванням `false`), сервісного доступу не існує, а штрихкодів мережа не віддає взагалі, тож у тексті її немає. З тієї ж причини сторінка не обіцяє й збирання списку покупок у кошик мережі: у межах названо лише те, чого продукт не робитиме ніколи (оформлення й оплату), без анонсу того, що за прапорцем.
- **Синхронізацію планів між пристроями.** Денний і тижневий плани живуть у сховищі пристрою, сервер їх не зберігає. Планів на сторінці немає, отже й обіцянки немає.
- **Спільну комору чи сімейний режим.** Код суто per-user, сімейності немає як стану.
- **Медичну точність.** Немає слів «норма», «здорове харчування», «правильно», немає підрахунку «скільки тобі треба» як рекомендації.
- **Однакові числа на всіх поверхнях.** Механізму звірки між поверхнями немає, тож твердження «скрізь однаково» на сторінці не звучить.
- **Голосовий ввід і AI-памʼять.** Обидва за вимкненими прапорцями.
- **Будь-яку соціальну валідацію.** Немає відгуків, кількості користувачів і «допомогло N людям»: зовнішніх користувачів нуль.

## Внутрішні лінки

**Звідси ведуть:**

| Куди                   | Звідки в тексті            | Роль лінка                                  |
| ---------------------- | -------------------------- | ------------------------------------------- |
| `/guides/kbzhv`        | секція «Далі про їжу»      | практика: що робити з кожним типом продукту |
| `/guides/foto-kalorii` | секція «Далі про їжу»      | межі фото-оцінки                            |
| `/data`                | мікрокопія під «Межі»      | куди їдуть фото і що бачить Sergeant        |
| `/zvyazky`             | хвіст сторінки             | звʼязки між модулями і право мовчати        |
| `/stan`                | хвіст сторінки             | пер-модульний статус                        |
| `/beta`                | хвіст сторінки, єдиний CTA | черга в Telegram                            |

**Сюди мають вести:**

- головна, блок модуля «Харчування» – з картки на сторінку модуля;
- `/guides/kbzhv` і `/guides/foto-kalorii` – хвостовим лінком замість прямого переходу в чергу;
- `/guides` – з картки харчових гайдів;
- `/stan` – з рядка про модуль Харчування;
- `/zvyazky` – коли йдеться про сигнали з їжі.

**Обережно:** `/zvyazky` і `/stan` на 2026-08-29 не існують. Якщо їх не буде на момент публікації `/yizha`, ці два лінки знімаються разом із реченням, що їх вводить, а не замінюються заглушкою.

**Чого свідомо немає:** лінка на сторінку Фініка чи Фізрука в тілі тексту. Сторінка відповідає на одне питання, і розсіювати її на сусідні модулі означало б повторити проблему головної, де всі теми конкурують за один документ.
