# Глибокий аудит: Профіль і Налаштування

> **Last touched:** 2026-08-08 by @claude. **Next review:** 2026-11-06.
> **Status:** Active — дослідження виконано, фіксів ще не було; план у §7.

> **Що це.** Наскрізний аудит двох поверхонь за
> [`deep-module-audit.md`](../../00-start/playbooks/deep-module-audit.md) —
> першим застосуванням playbook-а після його написання.
> **Профіль:** `apps/web/src/core/profile/` (6 секцій), рендериться як вкладка
> хаба, НЕ окремий route. **Налаштування:** `apps/web/src/core/settings/` +
> `core/hub/HubSettingsPage.tsx` — 14 секцій у 3 вкладках, **два** різні входи.

## 1. Методологія і покриття

- **Кодовий аудит:** read-only fan-out по шести вимірах (логіка профілю,
  логіка налаштувань, a11y, дизайн-система, дані/синхронізація, мертвий
  код) — по агенту на вимір.
- **Адверсарний пас:** окремий скептик на кожен вимір, чия задача —
  СПРОСТУВАТИ знахідки. Заявлено 86, **вистояло 78, спростовано 8**.
- **Критик повноти:** окремий прохід «чого не вистачає» поверх вистоялого.
  Знайшов ще **3 P1**, яких не побачив жоден finder — усі три про шляхи, а
  не про файли (див. §3, L-1…L-3).
- **Браузерна верифікація:** 390×844, демо, живий дев-сервер (бекенд
  недоступний — 502).

**Чого перевірити НЕ вдалося.** Профіль сидить за автентифікацією: у демо
замість `hub-tab-profile` рендериться `hub-tab-auth`, а бекенд у цьому
середовищі віддає 502. Тому **всі знахідки по Профілю — статичні**, з
репро-описом, але без живого підтвердження. Це головна діра покриття.

**Три власні хибні заміри, спіймані по дорозі** (лишаю як застереження
наступному, хто піде цим шляхом):

1. «11 перемикачів без доступного імені» — я міряв внутрішній трек
   `label[for]` 44×24, а справжнє імʼя дає обгортка-`<label>` навколо
   всього рядка. Перемикачі здорові.
2. «12 тач-цілей < 44px» — то були ті самі внутрішні треки, які не є
   самостійними цілями. Реальна ціль рядка — 93–132px.
3. «`/settings` має нижню навігацію» — `document.querySelector('nav')`
   знайшов футер «Юридичні документи». Навігації там справді немає.

## 2. Що працює (верифіковано наживо)

- **Перемикачі**: доступне імʼя через обгортку-`<label>`, клік по тексту
  рядка перемикає, тач-ціль рядка 93–132px, стан переживає reload навіть
  без бекенду.
- **Вкладки налаштувань**: справжній `tablist`/`tab` із робочим roving
  tabindex (`0/-1/-1`, стрілка рухає фокус).
- **Пошук по секціях**: знаходить секцію з **прихованої** вкладки
  («їжа» з вкладки «Розділи», коли відкрито «Загальні»); на беззмістовний
  запит — чесний порожній стан із «Очистити пошук»; «сповіщ» і «push»
  ведуть на ту саму секцію.
- **Перший рівень акордеонів**: усі 6 секцій мають `aria-expanded`.
- **Deep-link `?group=`**: `/settings?group=advanced` справді відкриває
  вкладку «Додатково».

## 3. Проблеми: логіка і дані

Нумерація стабільна — нею називатимуться коміти й PR-и.

### L-1 (P1) `/settings` — глухий кут без виходу

`core/settings/route.tsx:24-41` — весь файл це `<main>` + `HubSettingsPage`.
Ні хедера, ні «Назад», ні таббару: `HubBottomNav` рендериться лише з
`HubHomeView.tsx:177`, тобто на index/catch-all маршрутах. `pb-28` (112px)
резервує місце під навігацію, якої тут не існує.

**Верифіковано наживо:** на `/settings` єдиний `<nav>` — футер «Юридичні
документи»; усі `a[href]` ведуть на `/legal/*` або `#main`; контролів
«назад» — **0**. Вийти можна лише жестом браузера.

Три картки «Можливостей» ведуть саме сюди (`capabilityRegistry.ts:128,136,144`).

**Наслідок для решти аудиту:** будь-який фікс, перевірений у вкладці хаба,
для цієї оболонки НЕ перевірений — інший скролер, інші sticky-обчислення
(`HubSettingsPage.tsx:352-363` читає `scrollContainer.getBoundingClientRect()`),
немає `PullToRefresh`, інша десктопна ширина.

### L-2 (P1) Повернення з оплати нікуди не веде

`billing/stripe.ts:271`, `liqpay.ts:249`, `plata.ts:256` повертають
користувача на `/settings?billing=portal-return|manage`. Докстрінг над
стріпівським прямо каже, що веб має «refetch billing.status and reflect any
plan change immediately».

**Веб цього не робить.** `grep -rn 'billing=' apps/web/src` → рівно два
хіти, обидва — коментарі в `PricingPage.tsx:89,284`. Жодного
`searchParams.get("billing")`.

**Що бачить людина** після скасування підписки: холодне завантаження,
вкладка «Загальні», всі секції згорнуті (`defaultOpen = false`,
`SettingsPrimitives.tsx:76`), нуль підтверджень, що дія відбулась.
`PlanSection` має `anchorId`, але auto-open спрацьовує лише на **хеш**
(`matchesHash`, `:65-69`), а в return-URL — query.

Разом із L-1: це єдине місце обох поверхонь, куди приходять із **зовнішнього
origin**, і «назад» тут веде назад у платіжний портал.

### L-3 (P1) Екран згоди дефолтиться в «увімкнено», політика — opt-in

`PrivacySection.tsx:15-17` → `analytics: true`. Проти цього три джерела в
тому ж репо:

| Джерело                                     | Значення                                                                                                                   |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `server/modules/me/dataRights.ts:48`        | `analytics: false` + коментар «product policy is opt-in analytics, not opt-out… must move in lockstep with the DB DEFAULT» |
| `core/observability/analyticsConsent.ts:45` | `cachedAnalyticsConsent = false` + докстрінг «DENY UNTIL HYDRATED… This used to default `true`»                            |
| міграція 111                                | DB DEFAULT `FALSE`                                                                                                         |

Тобто єдине місце в стеку, яке дефолтиться в «згода є», — це екран, чия
робота показати, чи вона є. Loading-гейта немає: новий акаунт бачить
«Аналітика продукту: увімкнено», а після GET воно перекидається у
вимкнено. Офлайн/гість — лишається «увімкнено» назавжди поруч із текстом,
що керувати не можна.

### L-4 (P1) Біометрія: значення поза діапазоном СТИРАЄ збережене

`BiometricsSection.tsx:114-121` — `parseInRangeOrNull` віддає `null` і для
порожнього поля, і для «поза межами», і для сміття. Далі diff (`:142`,
`:167`) бачить `175 !== null` → `patch.heightCm = null` → **збережений зріст
стерто**, а `toast.success` на `:246` стоїть поза будь-якою гілкою.

Набрати «1750» замість «175» (зайва цифра на мобільній клавіатурі —
`max={260}` не блокує, бо форма не сабмітиться нативно) → зріст зникає,
тост каже «Біометрію збережено», error-стану в секції немає взагалі.

Далі по ланцюгу: `isBiometricsCompleteForTdee` → `false` → розрахунок КБЖВ
мовчки перестає працювати.

Власний коментар модуля (`:94-99`) каже «за межі не пускаємо взагалі, а не
клемпимо». Код не «не пускає» — він стирає.

**Корінь глибший за секцію** (з адверсарного пасу): `parseInRangeOrNull`
конфляє три різні стани в один `null`, тож diff за побудовою не відрізняє
навмисне очищення від відкинутого вводу. Фікс лише в `handleSave` буде
латкою — потрібен трирівневий парс `empty | invalid | value`.

Наявний тест (`BiometricsSection.test.tsx:221`) перевіряє випадок, коли
збереженого значення ще НЕ БУЛО — тобто рівно той, де регресії немає.

### L-5 (P1) Імпорт JSON перезаписує все без підтвердження

`HubBackupPanel.tsx:32` — вибір файлу одразу викликає
`applyHubBackupPayload` (`hubBackup.ts:131-160`), перетираючи витрати,
борги, звички, тренування й харчування чотирьох модулів, і робить reload.
Жодного діалогу, жодного «це замінить ваші дані».

### L-6 (P1) Скасування зміни PIN вимикає блокування

`RootLayout.tsx:142` + `AppLock.tsx:326` — один і той самий `onSetupCancel`
передається і для `setup`, і для `change`. Тап «Змінити PIN» → «Скасувати»
(або Escape) → блокування **вимкнено повністю**, старий PIN лишається в
сховищі.

### L-7 (P1) Згорнуті секції лишаються у tab-порядку

`CollapsibleSection.tsx:203` — вміст ховається візуально, але лишається у
дереві доступності. З Профілю: ~15 разів Tab від заголовка → фокус
всередині невидимих полів пароля, кнопки «Відкликати сесію» і
**destructive-кнопки «Видалити акаунт»** (`DangerZoneSection.tsx:89`), де
Enter наосліп відкриває діалог видалення.

### L-8 (P1) Банк памʼяті не має серверної ноги і гине при логауті

`purgeLocalData.ts:78` стирає `hub_user_profile_v1`. Пройдене інтервʼю з
асистентом зникає після виходу й не існує на другому пристрої; чат-тул
`my_profile` віддає «Профіль пам'яті порожній».

### L-9…L-20 (P2) — стисло

| ID   | Суть                                                                                         | Файл                                |
| ---- | -------------------------------------------------------------------------------------------- | ----------------------------------- |
| L-9  | Помилка збереження згоди НІКОЛИ не рендериться — тумблер мовчки відкочується                 | `PrivacySection.tsx:242`            |
| L-10 | Мертвий перемикач «Показувати підказки»: `showHints` не читає ніхто                          | `DashboardSection.tsx:34`           |
| L-11 | Після 10 невдалих PIN сховище стерто, а тумблер «Блокування» лишається ON                    | `PrivacySection.tsx:130`            |
| L-12 | Deep-link веде на ЗГОРНУТУ секцію — auto-open є у 3 із 14                                    | `PWASection.tsx:64`                 |
| L-13 | Пошук індексує лише рукописні keywords — цілі підрозділи недосяжні                           | `HubSettingsPage.tsx:263`           |
| L-14 | Гонка GET/PUT: тумблер застигає у старому значенні                                           | `useServerPreference.ts:45`         |
| L-15 | Помилка стану Monobank показується як «не підключено» і просить токен                        | `FinykWebhookServiceSection.tsx:74` |
| L-16 | «Пропустити» в ручній памʼяті = «Зберегти» (той самий обробник)                              | `MemoryBankSection.tsx:417`         |
| L-17 | Збереження ТІЛЬКИ ваги не пушить профіль на сервер                                           | `BiometricsSection.tsx:236`         |
| L-18 | «Завершити» поточну сесію стирає базу без гейта незбережених записів, який має «Вийти» поруч | `SessionsSection.tsx:132`           |
| L-19 | Офлайн «Активні сесії» показують «Немає сесій» — брехливий порожній стан                     | `SessionsSection.tsx:33`            |
| L-20 | «Очистити памʼять ШІ» не інвалідує `aiMemoryKeys` — список показує стерте                    | `PrivacySection.tsx:215`            |

### L-21 (P2) Демо: заміри Фізрука порожні через розбіжність полів

`seedFizruk.ts:136` пише `weight`/`waist`/`chest`, а адаптер
(`sqliteWriter/adapter.ts:574-576`) читає `weightKg`/`waistCm`/`chestCm` →
у SQLite лягають `NULL`. **Це наслідок нашої ж хвилі 5** (#749): доти демо
не доїжджало до модуля взагалі, тож розбіжність не проявлялась. Той самий
клас, що й ключі мʼязів, які ми вже виправили.

## 4. Дизайн-дослідження: структура налаштувань

**Інвентар «Загальні» на 390×844** (виміряно): 6 рядків-акордеонів по
**64px**, пошук зверху, вкладки під ним. Уся вкладка займає ~половину
екрана, решта — порожньо. Секції згорнуті за замовчуванням.

**Акордеон усередині акордеона.** Щоб дійти до перемикача «Чистий режим»,
треба **два тапи**: «Дашборд» (64px, має `aria-expanded`) → «Вигляд»
(41px, `aria-expanded` **відсутній**). Два рівні однієї ролі оформлені
різними патернами й різними висотами.

**Два входи, дві оболонки.** Вкладка хаба має нижню навігацію, хедер і
`role="tabpanel"` (`hub-panel-settings`). Роут `/settings` — нічого з
цього (L-1). Причому в хабі панель є, а у вкладках самих налаштувань —
немає (V-1).

**Стрілка `>` на рядках** читається як навігація «перейти», хоча це
розкриття на місці.

## 5. Візуальні проблеми

| ID            | Суть                                                                                                                             | Замір / файл                               |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| **V-1** (P2)  | Вкладки — `tablist` без жодного `tabpanel` і без `aria-controls`; у хабі поруч панель Є                                          | виміряно: `panel: false`, `controls: null` |
| **V-2** (P2)  | Другий рівень акордеонів без `aria-expanded`                                                                                     | 8 кнопок, усі `null`                       |
| **V-3** (P2)  | Тач-цілі другого рівня нижчі за floor                                                                                            | 40–41px проти 44 під `pointer:coarse`      |
| **V-4** (P2)  | Кожна секція Профілю показує заголовок **двічі**, і зовнішній дрібніший за внутрішній — інверсія ієрархії                        | `ProfilePage.tsx:137`                      |
| **V-5** (P2)  | Банер «Експериментальних» намальований неіснуючим токеном `warn` — рендериться без стилю                                         | `ExperimentalSection.tsx:61`               |
| **V-6** (P2)  | Незворотне «Очистити памʼять ШІ» підтверджується нативним `window.confirm` — патерн, який дизайн-система позначає як заборонений | `PrivacySection.tsx:210`                   |
| **V-7** (P2)  | Індекс і секції називаються по-різному в 3 із 14; дві секції в підсумку майже однойменні                                         | `HubSettingsPage.tsx:208`                  |
| **V-8** (P2)  | Чотири різні оболонки підтвердження з трьома різними затемненнями на двох сторінках                                              | `DeleteAccountDialog.tsx:44`               |
| **V-9** (P2)  | Факти памʼяті — контент, заради якого секція існує — обрізані `truncate` без `title`                                             | `MemoryBankSection.tsx:311`                |
| **V-10** (P2) | Профіль вужчий за Налаштування на 40px і має вдвічі щільніші проміжки — сусідні вкладки одного хаба                              | `ProfilePage.tsx:96`                       |
| **V-11** (P2) | Два входи в «що ШІ про мене знає» з різними назвами, формою рядка й повнотою тексту                                              | `AiMemoryList.tsx:121`                     |
| **V-12** (P3) | «Підблок усередині секції» намальовано **пʼятьма** різними рецептами замість примітиву                                           | `SettingsPrimitives.tsx:163`               |
| **V-13** (P3) | Вкладка «Розділи» — чотири візуально ідентичні сірі рядки: `module`-акцент не передає жодна секція                               | `SettingsPrimitives.tsx:40`                |
| **V-14** (P3) | Три порожні стани — три різні рецепти попри спільний `EmptyState`                                                                | `MemoryBankSection.tsx:249`                |
| **V-15** (P3) | Skeleton lazy-секції резервує 168–280px під секцію, що розкривається на ~72px — стрибок у зворотний бік                          | `HubSettingsPage.tsx:258`                  |
| **V-16** (P3) | Хрестик «Очистити пошук» зникає саме тоді, коли нічого не знайдено                                                               | `HubSettingsPage.tsx:393`                  |

## 6. Кодовий аудит: борг

- **Пошук ⌘K розійшовся з реальністю**: `searchSettings.ts:32` містить 2
  неіснуючі секції, а 4 реальні (`plan`, `privacy`, `feedback`,
  `capabilities`) відсутні. `target.sectionId` формується для кожного
  хіта, але навігація його ігнорує (`useSearchEngine.ts:181`).
- **Найнебезпечніші дії без тестів**: видалення акаунта (мок `deleteUser`
  заведено й жодного разу не перевірений), гейт «є незбережені записи» на
  виході, `DashboardSection` цілком, пошук по секціях.
- **Застарілі коментарі**, що описують неіснуюче: `route.tsx:22` обіцяє
  `useHubShell()`, якого у файлі немає; `SettingsPrimitives.tsx:38` двічі
  посилається на Hard Rule #12, retired ADR-0081; `useSearchEngine.ts:181`
  описує обмеження, якого вже нема. **Той самий клас, що вбив демо-режим
  у попередній хвилі** — коментар пережив механізм.
- **Два collapsible-примітиви** з різною поведінкою: Профіль памʼятає
  відкрите, Налаштування — ні (`SettingsPrimitives.tsx:79`).
- `eslint.i18n-allowlist.json:129` містить два неіснуючі файли, а
  коментар-власник бреше про розмір у 10 разів.

## 7. Пріоритезований план

**Quick wins (≤ пів дня, без продуктових рішень):**

1. **L-4** трирівневий парс біометрії + error-стан поля (не латка в `handleSave`).
2. **L-6** окремий `onCancel` для `change` — не чіпати стан блокування.
3. **L-5** діалог підтвердження перед імпортом JSON.
4. **L-7** `inert`/`hidden` на згорнутий вміст.
5. **V-2**/**V-3** `aria-expanded` + 44px на другий рівень акордеонів.
6. **V-5** замінити фантомний токен `warn`.
7. **L-10** прибрати мертвий `showHints` (або дати йому читача).
8. **V-1** `tabpanel` + `aria-controls` у вкладках налаштувань.

**Середні:**

9. **L-3** дефолт згоди → `false` + loading-гейт (юридично чутливе — узгодити).
10. **L-2** читач `?billing=` з рефетчем статусу й підтвердженням.
11. **L-9**, **L-11**, **L-14**, **L-15** — чесні стани помилок.
12. **L-13**/**борг ⌘K** — один спільний індекс секцій замість двох.
13. **L-21** привести демо-заміри до канонічних імен полів.
14. **V-6**/**V-8** — одна оболонка підтвердження замість чотирьох + `window.confirm`.
15. Тести на найнебезпечніші дії (видалення акаунта, unsynced-гейт).

**Великі (потребують рішення власника):**

16. **L-1** — чим є `/settings`: повноцінним екраном із хромою, редиректом
    у вкладку хаба, чи модальним шаром? Зараз це третій стан — глухий кут.
17. **L-8** — серверна нога для банку памʼяті або чесна копірайт-обіцянка
    «лише на цьому пристрої».
18. **V-4**/**V-10** — привести Профіль і Налаштування до однієї сітки.
19. **§4** — чи потрібні два рівні акордеонів; чи не варто розкривати
    першу секцію за замовчуванням.

## 8. Як відтворити ключові баги

- **L-1**: `/settings` напряму → спробувати вийти в застосунок без жесту «назад».
- **L-3**: новий акаунт або офлайн → Налаштування → Конфіденційність →
  «Аналітика продукту» стоїть увімкненою.
- **L-4**: Профіль → Біометрія → зберегти зріст 175 → набрати 1750 →
  «Зберегти» → поле порожнє, тост «Збережено».
- **L-6**: увімкнути блокування → «Змінити PIN» → «Скасувати» → блокування зникло.
- **L-7**: Профіль, нічого не розгортати → Tab ×15 → фокус на «Видалити акаунт».
- **V-2**/**V-3**: Налаштування → «Дашборд» → заміряти заголовок «Вигляд»
  (41px, `aria-expanded === null`).

---

**Спростовано адверсарним пасом (8)** — лишаю, щоб не переоткривали:
біометрія офлайн (навмисно, задокументовано), ПриватБанк-секція за
прапорцем (задокументовано), поле токена Monobank (плейсхолдер — легітимне
джерело імені), офлайн-банер Профілю (канонічний має `aria-live` вище по
стеку), ім'я поля пошуку (перевірено через `dom-accessibility-api`),
переповнення ряду дій памʼяті (глобальний safety-net 44px у `mobile.css`),
дублювання `useServerPreference` в `PrivacySection` (свідомо відкладено),
відʼєднання ПриватБанку (недосяжно).
