# Mockups · Backlog

> **Last validated:** 2026-08-16 by @claude (звірено з деревом `mockups/` — усі 4 «заплановані» поверхні й уся міграція структури виявились закритими; цифри кластерів за 2026-05-18 лишаю як історичний зріз, живий стан — портал).
> **Next review:** 2027-04-01.
> **Status:** Active.
> **Maintainer:** founder + Claude (designer-assistant).

Що зроблено у `/mockups/` і що ще потрібно — щоб можна було повернутись і
продовжити з того самого місця. Не «беклог завдань», а **знімок стану
візуальної бібліотеки** на дату вище.

Кросс-посилання:

- `mockups/index.html` — живий портал зі статусами кожної поверхні.
- `mockups/README.md` — структура папок, правила, промпт-шаблони для агентів.
- ~~`Mockups Audit.html`~~ — разовий аудит трьох розкиданих кластерів,
  замінений цим документом. У репо файлу вже немає (перевірено 2026-08-16).

---

## Зведено · 2026-05-18 (історичний зріз)

> Таблиця нижче — стан на 18 травня, і вона знає лише **чотири** кластери.
> Станом на 2026-08-16 їх **шість**: до `landing` (12 html), `marketing`
> (17), `flows` (4) і `product` (40) додались `pitch` (3) і `motion` (1).
> Обидва нові — набори окремих файлів без власного `index.html`.
> Колонка «Заплановано» вже порожня — див. § «Раніше заплановане».
>
> Перераховувати «поверхні» тут навмисно не став: одна поверхня — це тека з
> `index.html`, а більшість нових файлів у `product/` це варіанти й досліди,
> тож механічний перелік дав би хибну точність.
> **Живий стан — `mockups/index.html`.**

| Кластер                                                 | Усього | У продукті | Заплановано |
| ------------------------------------------------------- | -----: | ---------: | ----------: |
| Лендинг (3 напрямки + 8 кампаній)                       |     11 |         11 |           — |
| Маркетинг (листи / соцмережі / OG / прес / магазин / …) |     12 |         12 |           — |
| Продукт (модулі + основні поверхні)                     |     11 |          8 |           3 |
| Потоки (реєстрація / реферал / n8n / Telegram-бот)      |      4 |          3 |           1 |
| **Разом**                                               | **38** |     **34** |       **4** |

---

## Зроблено · з найважливішого

### M-1 · Портал v0.2 — `mockups/index.html`

Sticky topbar з jumps та пошуком. 4 кластери з картками, фільтр статус ×
пріоритет, легенда трьох кольорів. Замінив три розкидані індекси
(старий `/mockups/index.html`, `landing-mockups/README.md`,
`handoff-package/Index.html`).

### M-2 · Стани — `mockups/product/states/index.html`

3 рівні з `docs/05-design/design/empty-states.md`: повноекранний (4 модулі) /
компактний (2 приклади) / рядком (3 приклади) + 3 види помилок
(синхр., дозвіл, офлайн) + 4 «сьогодні нуль». Анатомія
`<ModuleEmptyState>` з 6 слотами + 4 «так / не так» приклади з коду.

### M-3 · Первинне знайомство — `mockups/product/onboarding/index.html`

6 кроків × 3 стани (Зараз / Планується / Рекомендую). Джерело —
`docs/01-product/launch/product-os/ftux-master-tracker.md`. Воронка з 8 PostHog
подій + 3 SLO (≥30% активація, ≤90с TTV, ≥25% D1).

### M-4 · Платний доступ — `mockups/product/paywall/index.html`

Sheet після першого запису (4 сек після CelebrationModal). 3 варіанти
копії α/β/γ. Матриця 5 кандидатів розміщення з оцінками (B обрано,
A/E відхилено, C/D — фаза 2). Воронка 5 PostHog подій. Джерело —
`docs/01-product/launch/product-os/paywall-ux-placement.md`.

### M-5 · HubChat — `mockups/product/hubchat/index.html`

3 стани × 3 секції (порожній / відповідь / крос-модульне). Джерело —
реальний код `apps/web/src/core/hub/chat/` (7 файлів) + плани з
`cross-module-prompts.md` і `undo-pattern.md`. Таблиця 5 можливостей
× 3 стани.

### Інфраструктура

- `_shared/tokens.css` — дзеркало `docs/05-design/design/design-system.md`.
- `_shared/marketing.css` — кремові розкладки.
- `_shared/product.css` — темна v2 з glass.
- `_shared/code-references.md` — мапа мокап ↔ файл коду.

---

## Раніше заплановане · усе закрито

Ревалідація 2026-08-16: усі чотири поверхні існують на диску. Рядки лишені
з датами закриття, щоб було видно, що беклог порожній, а не забутий.

| #   | Поверхня                                                        | Файл на диску                     | Стан |
| --- | --------------------------------------------------------------- | --------------------------------- | ---- |
| 1   | **Налаштування + приватність** `product/settings/`              | `mockups/product/settings/`       | ✅   |
| 2   | **Швидкий запис · плаваюча кнопка + лист** `product/quick-add/` | `mockups/product/quick-add/`      | ✅   |
| 3   | **Push-сповіщення** `product/push/`                             | `mockups/product/push/`           | ✅   |
| 4   | **Telegram-бот · сценарій** `flows/telegram-bot.html`           | `mockups/flows/telegram-bot.html` | ✅   |

Понад цей список у `product/` з травня зʼявились ще `nudges/` і `splash/`
(теки з `index.html`), плюс великий пласт одиничних досліджень — від
`anti-slop-*` і `hub-axes-*` до `number-typography.html`. У беклог їх не
заводжу: він про заплановані поверхні, а не про реєстр усього намальованого.

### Окремо · поза цим беклогом, але згадане у початковому аудиті

| #   | Поверхня                            | Чому не у цьому беклозі                                                                                                                |
| --- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 5   | Крос-модульні підказки (anti-nag)   | Часткове покриття у M-5 «Рекомендую». Окремий мокап на основі `cross-module-prompts.md` — після того як паттерн стабілізується у коді. |
| 6   | Деталі модулів (3 екрани на модуль) | 12-16 мокапів. Велика робота — пакувати у наступний цикл (post-launch).                                                                |
| 7   | Планшетні / десктопні екрани        | На launch — лише мобільні; десктоп після.                                                                                              |
| 8   | Розширені транзакційні листи        | Розширення `marketing/email-drip.html` — додати verify / reset / receipt / referral-milestone.                                         |
| 9   | Темні маркетинг-варіанти            | 1 лендинг + 1 OG-картка у темі — для Telegram-превʼю.                                                                                  |
| 10  | Прес-кіт пакет ассетів (PNG/SVG)    | Експорт-готові логотипи й скріни. Технічна задача, не дизайнерська.                                                                    |

---

## Що змінилось у структурі від початкового аудиту

| Звідки                                                     | Куди                                             | Статус |
| ---------------------------------------------------------- | ------------------------------------------------ | ------ |
| `docs/01-product/launch/landing-mockups/mockup-NN-*.html`  | `mockups/landing/campaigns/<slug>.html`          | ✅     |
| `mockups/landing/v*.html`                                  | `mockups/landing/directions/v*.html`             | ✅     |
| `docs/05-design/design/redesign-v2/handoff-package/*.html` | `mockups/product/{module}/*.html`                | ✅     |
| `mockups/shared.css`                                       | `mockups/_shared/{tokens,marketing,product}.css` | ✅     |
| `mockups/{email-drip, social-posts, …}.html`               | `mockups/marketing/<same>.html`                  | ✅     |
| `mockups/{signup-flow, referral-flow, n8n-flow}.html`      | `mockups/flows/<same>.html`                      | ✅     |
| Старий `mockups/index.html` (19кб)                         | Замінений новим порталом                         | ✅     |
| Старий `mockups/shared.css` (5кб)                          | Видалений, працює `_shared/marketing.css`        | ✅     |
| Старий `mockups/landing/README.md`                         | Видалений (замінив `mockups/README.md`)          | ✅     |

Перевірка 2026-08-16 стосувалась пʼяти останніх рядків таблиці — тих, що до
цієї ревалідації стояли зі статусом 🟡. Перші чотири (переїзди кампаній,
напрямків, handoff-пакета й `shared.css`) були позначені ✅ ще у травні і
наново не звірялись.

Що саме перевірено: у корені `mockups/` лишився єдиний `index.html`,
`shared.css` і `landing/README.md` відсутні, теки `marketing/` і `flows/`
наповнені.

---

## Як повернутись і продовжити

1. **Відкрий портал:** `mockups/index.html`. Дивись на картки зі статусом
   «заплановано» — вони з пунктирним штрихуванням.
2. **Спершу — найвищий пріоритет.** Картки з номером 2 «високо».
3. **Знайди план у `docs/01-product/launch/product-os/` або `docs/05-design/design/`** — у картці
   на порталі є посилання на джерело-план.
4. **Намалюй мокап** за тією ж конвенцією — 3 стани (Зараз / Планується /
   Рекомендую) для продуктових екранів. Збережи у `mockups/product/<slug>/index.html`.
5. **Зміни статус** у картці порталу на «у продукті». Бамп
   `last-validated` у footer мокапу і `stat-done` у hero порталу.
6. **Онови цей беклог** — перенеси рядок з «не зроблено» у «зроблено».

---

## Дрібні правки / техдебт у самих мокапах

| #   | Що                                                                       | Де                       |
| --- | ------------------------------------------------------------------------ | ------------------------ |
| 1   | ~~`Mockups Audit.html` у корені~~ — файлу немає, пункт закрито           | ✅ перевірено 2026-08-16 |
| 2   | Перевірити з мобілки що 3-колонковий layout не ламається на 360px        | усі продуктові мокапи    |
| 3   | ~~Лінк зі старого `landing/README.md`~~ — файл видалено, редирект зайвий | ✅ перевірено 2026-08-16 |
| 4   | `last-validated` дати скрізь — 2026-05-18                                | усі мокапи M-2..M-5      |
| 5   | `_shared/code-references.md` додати hubchat-файли                        | code-references          |
