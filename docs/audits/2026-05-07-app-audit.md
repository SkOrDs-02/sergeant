# Sergeant — повний аудит застосунку (2026-05-07)

> **Last validated:** 2026-05-07 by @Skords-01 / Devin. **Next review:** 2026-08-05.
> **Status:** Active

> Аудит виконано 2026-05-07 проти `main @ 8e94b06b` за запитом «давно не заходив в апку, поки працювали з кодом — там купа регресій, багів і тд. роби повний аудит та тест». Скоуп — типечек, лінт, юніт-тести, ручний бут web-аппа, sanity-check API, PR-archaeology і scan дзеркальних регресій. **Жодних код-змін у цьому документі — лише трасинг.** Follow-up PR-плани живуть у §10.
>
> **Сесія:** https://app.devin.ai/sessions/291d0c8ee2bb4e24802c85f61eb0f03f
> **Запис (blank-app + console error + DOM):** https://app.devin.ai/attachments/c989b966-fd70-4ae6-9550-f8dc00bfe498/rec-653255861e684fda94112ab101e85989-edited.mp4

> ⚠️ **Главне:** web-додаток **ВЗАГАЛІ НЕ ЗАВАНТАЖУЄТЬСЯ** через одну рядкову регресію. Будь-який URL (включно з `/welcome?demo=1`) показує білий екран — не показує навіть error-boundary. Ця проблема зашиває ВЕСЬ ручний користувацький тест: жоден потік (Finyk / Fizruk / Routine / Nutrition / HubChat) недосяжний з браузера.

---

## TL;DR — Severity-Map

| #   | Severity       | Сурфейс                 | Що зламано                                                                                      | Вердикт                                                                                 |
| --- | -------------- | ----------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| 1   | **🔴 BLOCKER** | `apps/web`              | Бут крашиться через `node:fs` у бандлі (`kvStoreBoot.ts:41` тягне umbrella `db-schema/migrate`) | **Хотфікс одного імпорту** + ESLint guard / drop umbrella                               |
| 2   | 🟠 High        | `apps/mobile` typecheck | Орфан-тест `identifyTraits.test.ts` без імплементації                                           | Видалити тест або відновити модуль                                                      |
| 3   | 🟠 High        | `apps/mobile` tests     | 9 suites / 48 tests падають (Finyk/Routine/Nutrition/Budgets/Transactions/Onboarding/Analytics) | **4 root-cause'и** (див. §4) — два фіксяться однією util-функцією + перейменуванням     |
| 4   | 🟠 High        | `apps/web` tests        | 3 з 9 тестів `useAppLock.test.ts` падають                                                       | **Не логіка апа** — Node22/jsdom WebCrypto cross-realm типізація салту (див. §3)        |
| 5   | 🟡 Medium      | Lint hard-rule          | `process.env` budget overflow: 120 > 119 (+1)                                                   | Hard-rule violated → лінт червоний                                                      |
| 6   | 🟡 Medium      | Server warnings         | `REDIS_URL`, `NUTRITION_BACKUP_KEY_SECRET`, `BETTER_AUTH_TOKEN_ENC_KEY` не задані               | Очікувано в dev, але `NUTRITION_BACKUP_KEY_SECRET` дає 503 на `/api/nutrition/backup-*` |
| 7   | 🟢 Low         | Engine warnings         | `unsupported engine: wanted 20.x, current 22.12.0` × 6 пакетів                                  | Косметика; pinned 20.x у `engines`, локально стоїть 22                                  |
| 8   | 🟡 Latent      | `apps/mobile`           | 5 мобільних споживачів `db-schema/migrate` umbrella — латентний дзеркальний #1                  | Привести усі 6 (1 web + 5 mobile) до `/migrate/runner`                                  |

---

## 1. 🔴 BLOCKER — Web bootstrap дохне (`node:fs` у клієнтському бандлі)

**Файл:** [`apps/web/src/core/db/kvStoreBoot.ts`](../../apps/web/src/core/db/kvStoreBoot.ts) (рядок 41)
**Симптом:** перехід на будь-який URL (`/welcome?demo=1`, `/`, etc.) дає білу сторінку. `<div id="root"></div>` залишається порожнім. Error-boundary не встигає рендеритись, бо crash станеться на стадії модульного парсу.

### Що бачиться у DevTools

```
Uncaught Error: Module "node:fs" has been externalized for browser compatibility.
Cannot access "node:fs.promises" in client code.
  at Object.get (__vite-browser-external:node:fs:3:11)
  at files.ts:1:32
```

Vite-сервер дзеркалить це у логах:

```
[Unhandled error] Error: Module "node:fs" has been externalized for browser compatibility.
  Cannot access "node:fs.promises" in client code.
 > Object.get ../../../../../../@id/__vite-browser-external:node:fs:3:11
 > ../../packages/db-schema/src/migrate/files.ts:1:31
```

DOM (порожній root):

```html
<body>
  <div id="root"></div>
  <!-- React ніколи не отрендерив, бо crash станеться на стадії модульного парсу -->
</body>
```

### Кореневa причина

```ts
// apps/web/src/core/db/kvStoreBoot.ts:32–41
import {
  KV_STORE_CLIENT_MIGRATIONS,
  KV_STORE_MIGRATIONS_TABLE,
} from "@sergeant/db-schema/sqlite";
import { kvStore } from "@sergeant/db-schema/sqlite";
import {
  createSqliteAdapter,
  type SqliteMigrationClient,
} from "@sergeant/db-schema/migrate/sqlite";
import { runMigrations } from "@sergeant/db-schema/migrate"; // ← BUG
```

Імпорт іде з umbrella-сабпасу `@sergeant/db-schema/migrate`, який у `packages/db-schema/src/migrate/index.ts` re-експортить **усе**, включно з `loadMigrationFiles` із `./files.ts` — а той топ-левел імпортить `node:fs/promises` + `node:path`:

```ts
// packages/db-schema/src/migrate/files.ts:1–2
import { promises as fs } from "node:fs";
import path from "node:path";
```

Бандлер тягне за гілкою → у браузері `loadMigrationFiles` намагається доторкнутися `node:fs.promises` → виняток на boot.

### Це **не нова** пастка — її уже знає весь репозиторій

Інші браузерні споживачі ([`apps/web/src/modules/routine/lib/clientMigrate.ts`](../../apps/web/src/modules/routine/lib/clientMigrate.ts), [`apps/web/src/modules/fizruk/lib/clientMigrate.ts`](../../apps/web/src/modules/fizruk/lib/clientMigrate.ts)) вже мігрували на безпечний саб-сегмент і навіть лишили docstring-warning:

```ts
// apps/web/src/modules/routine/lib/clientMigrate.ts:5–11
// Import the runner from the dedicated `./migrate/runner` sub-path
// rather than `./migrate`. The umbrella `./migrate` entry re-exports
// `loadMigrationFiles` from `./files.js`, which top-level imports
// `node:fs` / `node:path` and breaks Vite's browser bundle when the
// SPIKE library lands in the production graph (see PR #1378
// follow-up). The runner itself is dialect- and platform-free.
import { runMigrations } from "@sergeant/db-schema/migrate/runner";
```

…а от свіжий PR #062 (`kvStoreBoot.ts`) цей урок проігнорував.

### PR-archaeology блокера

| Комміт     | Дата                  | Автор     | Що зробили                                                                                                                                                                                                                                                                     |
| ---------- | --------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `07baa1a1` | 2026-05-07 11:40 UTC  | Devin AI  | **Створив** `apps/web/src/core/db/kvStoreBoot.ts` із поганим імпортом `from "@sergeant/db-schema/migrate"` (PR #062 первинний)                                                                                                                                                 |
| `0c017523` | 2026-05-07 14:45 EEST | Skords-01 | Squash-merge PR #062 (`#2159`) на main — імпорт прийшов як є                                                                                                                                                                                                                   |
| `6cf3977c` | —                     | —         | PR #063: `webKVStore` swap на SQLite-backed (вирівняв `resolveStore()` тягнути SQLite першим, LS — фолбеком)                                                                                                                                                                   |
| `90c5626a` | 2026-05-07 12:52 UTC  | Devin AI  | **PR #064**: «drop LS mirror in webKVStore (SQLite-only)». Прибрав three-rung ladder → залишив SQLite-first → LS-fallback. **Тут і ховається експлоїт**: до цього PR-у LS-fallback маскував будь-який збій SQLite-гілки, тепер SQLite-гілка **єдина**, її падіння = blank app. |

**Отже:** регресія створена `07baa1a1`, **детонувала** на проді з `90c5626a`. Між створенням бомби і її детонацією — рівно 24 години. Жоден з 4 ревʼюверських сесій PR-#062/#063/#064 не зловив (чому — в §8).

### Запропонований фікс (1 рядок)

```diff
-import { runMigrations } from "@sergeant/db-schema/migrate";
+import { runMigrations } from "@sergeant/db-schema/migrate/runner";
```

### Гард, щоб не повторилось (опції в порядку зростання строгості)

1. **CI smoke**: `pnpm --filter @sergeant/web build` + перевірка вихідного бандлу на `vite-browser-external:node:` — лове **конкретний цей баг** на CI.
2. **ESLint `no-restricted-imports`** на `apps/web/**/*.{ts,tsx}` + `apps/mobile/**` забороняє `@sergeant/db-schema/migrate` без саб-сегмента.
3. **Drop the umbrella entirely** із `packages/db-schema/package.json` exports — найрадикальніший і **найнадійніший** варіант. Залишити тільки `./migrate/runner`, `./migrate/sqlite`, `./migrate/pg`, `./migrate/files` (Node-only).

---

## 2. 🟠 High — `apps/mobile` typecheck падає (orphan тест)

```
src/observability/identifyTraits.test.ts(28,37):
  error TS2307: Cannot find module './identifyTraits' or its corresponding type declarations.
```

У теці `apps/mobile/src/observability/` лежить **тільки** `IdentityBridge.tsx`. Файл `identifyTraits.ts` (або `.tsx`) — відсутній. Тест **завжди** не зкомпілиться.

Класична регресія типу «файл переіменували, тест осиротів». Або тест треба видалити, або файл повернути.

CI це би зловив (`pnpm typecheck` exit code 2), отже або CI по гілці впав і змерджили з override, або job недодивляється на `apps/mobile`.

---

## 3. 🟠 High — `apps/web` Vitest: 3 фейли в `useAppLock`

[`apps/web/src/core/security/useAppLock.test.ts`](../../apps/web/src/core/security/useAppLock.test.ts):

```
❯ src/core/security/useAppLock.test.ts (9 tests | 3 failed) 61ms
  × returns 'locked' when flag enabled and PIN is stored (6ms)
  × unlock() with correct PIN transitions to 'idle' and returns true (2ms)
  × unlock() with wrong PIN keeps 'locked' and returns false (3ms)
```

### Реальний traceback (вийняв через `pnpm vitest run … --reporter=verbose`)

```
TypeError: Failed to normalize algorithm:
  'salt' of 'Pbkdf2Params' (passed algorithm) is not instance of
  ArrayBuffer, Buffer, TypedArray, or DataView.
  ❯ deriveHash src/core/security/lockStorage.ts:40:36
  ❯ Module.savePinHash src/core/security/lockStorage.ts:62:16
```

### Конкретна проблема

```ts
// apps/web/src/core/security/lockStorage.ts:40–49
const bits = await crypto.subtle.deriveBits(
  {
    name: "PBKDF2",
    salt: salt.buffer as ArrayBuffer, // ← B
    iterations: 200_000,
    hash: "SHA-256",
  },
  keyMaterial,
  256,
);
```

`salt: salt.buffer as ArrayBuffer` — на Node 22 + jsdom + Vitest, `Uint8Array.buffer` віддає об'єкт із "чужого" realm-у, тому `instanceof ArrayBuffer` всередині WebCrypto false-ить. Якщо передати `salt` напряму (TypedArray), рантайм приймає.

### Це _не_ регресія user-facing-логіки

- `useAppLock.ts` сам по собі OK.
- В **продакшен-Chromium** код працює (`crypto.subtle.deriveBits` з `Uint8Array.buffer` приймається V8/Blink).
- Падає лише **тест** через node-jsdom особливість.
- Але **CI блокує мерж** через цю помилку, тому фактично — баг гілки, але не баг застосунку.

### Що це говорить нам про процес

Тест PR-1a UX-roast (`63c2858f feat(web): add app-lock base PIN flow`) додано **сьогодні** (`2026-05-07 12:54 UTC`, автор Claude AI). Він ніколи не проганявся локально на Node 22. Це натяк на процес: PR-и UX-roast серії мерджаться без локального `pnpm test`.

**Рекомендація:** простий fix потім (`salt: salt as BufferSource`), але важливіше — додати в playbook UX-roast явну вимогу `pnpm --filter @sergeant/web test` перед мерджем.

---

## 4. 🟠 High — Mobile test failures: таксономія по 4 root-cause'ах

48 фейлів у 9 suite-ах звучать як хаос — насправді все зводиться до **4 шваів**:

| Root cause                                        | Файли                                                                                                                    |          Тестів | Як фіксити                                                                                                  |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------: | ----------------------------------------------------------------------------------------------------------- |
| Missing `<ApiClientProvider>` у тестовому wrapper | `FinykApp.test.tsx`, `NutritionApp.test.tsx`, `RoutineApp.test.tsx`, `BudgetsPage.test.tsx`, `TransactionsPage.test.tsx` |             ~36 | Один util `renderWithProviders` у `apps/mobile/test-utils/`                                                 |
| Jest.mock() out-of-scope var                      | `src/lib/__tests__/identifyTraits.test.ts`, `src/observability/identifyTraits.test.ts`                                   | suite-level (2) | Перейменувати фабрику з `getVibePicksMock` на `mockGetVibePicks` (Jest дозволяє `mock`-префікс)             |
| Orphan-тест на видалений модуль                   | `src/observability/identifyTraits.test.ts` + (ймовірно) `src/features/analytics/AnalyticsIdentityBridge.test.tsx`        | suite-level (2) | Видалити тести або відновити модуль                                                                         |
| Missing `act()` wrap                              | `src/core/OnboardingWizard.test.tsx`                                                                                     |               1 | Обгорнути `await act(async () => {…})` навколо async-effect від `AccessibilityInfo.isReduceMotionEnabled()` |

### 4.1 `useApiClient` без `<ApiClientProvider>`

```
useApiClient must be used inside <ApiClientProvider>.
Create an ApiClient with createApiClient() and wrap the tree.

  at useFinykDualWriteBoot (src/modules/finyk/hooks/useFinykDualWriteBoot.ts:34)
  at FinykApp (src/modules/finyk/FinykApp.tsx:31)
```

Звинувачується свіже додавання `useFinykDualWriteBoot` → `useUser()` → `useApiClient()` у `FinykApp.tsx:31`. Усі тести, що рендерять `<FinykApp/>` напряму або сурогатні shell'ні (Nutrition/Routine/Budgets/Transactions — **усі** вантажать FinykApp в-стек), діло одне і те ж.

Імовірно, прихід `useFinykDualWriteBoot` — це частина dual-write rollout, тестовий сетап забули апдейтити.

### 4.2 Babel-hoisting `jest.mock(..., factory)`

```
ReferenceError: ... The module factory of `jest.mock()` is not allowed
to reference any out-of-scope variables. Invalid variable access:
getVibePicksMock
```

Класичний Jest gotcha: `jest.mock()` піднімається на верх файлу, перед імпортами. Змінна `getVibePicksMock` ще не існує, коли мок виконується. Jest **дозволяє** out-of-scope варіабли тільки якщо вони починаються з префікса `mock` (case-insensitive).

### 4.3 Orphan-тест без імплементації

```
src/observability/identifyTraits.test.ts(28,37): error TS2307:
  Cannot find module './identifyTraits' or its corresponding type declarations.
```

(Той самий orphan, що й у §2.) `identifyTraits.ts` мигрували в `IdentityBridge.tsx` — нові тести на новий API не написали, старі не прибрали.

### 4.4 Async-leak / `act()` в `OnboardingWizard.test.tsx`

```
Warning: An update to OnboardingWizard inside a test was not wrapped in act(...).
```

`AccessibilityInfo.isReduceMotionEnabled()` resolves після того, як test уже закрився. У production коді це не проблема (React state update in concurrent batch), у тесті — потребує обгортки.

### 4.5 Епілог mobile-тестів

```
Jest did not exit one second after the test run has completed.
'This usually means that there are asynchronous operations that
weren't stopped in your tests.
```

Окрім фактичних фейлів, мобільний test-suite має **leaked async**. На CI це або timeout, або flaky.

---

## 5. 🟡 Medium — Lint hard-rule violation: `process.env` budget overflow

```
✗ Disallowed 'process.env' reads grew: 120 > budget 119 (delta +1)
```

Список свіжих читань `process.env` (27 рядків у `apps/server/src/obs/{logger,metrics,tracing}.ts` + `apps/server/src/push/*` + `apps/server/src/routes/push.ts`).

Hard-rule вимагає міграції на Zod-валідовану `env.ts`:

> Migrate at least 1 read(s) to `import { env } from "../env.js"` (or `./env/env.js`) so values pass through Zod validation + defaults. Phase-2 plan: docs/initiatives/stack-pulse-2026-05/pr-01-unify-env-modules.md

Тобто `pnpm lint` червоний → блокує мерж по конвенції AGENTS.md.

---

## 6. 🟡 Medium — Server boot warnings (шум, але передбачуваний)

```json
{"msg":"env_warning","detail":"REDIS_URL is not set — rate limiting falls back to in-memory (per-process, not global)."}
{"msg":"env_warning","detail":"NUTRITION_BACKUP_KEY_SECRET is not set — /api/nutrition/backup-{upload,download} will return 503."}
{"msg":"env_warning","detail":"BETTER_AUTH_TOKEN_ENC_KEY is not set — OAuth tokens will be stored as plaintext (insecure; allowed in dev only)."}
```

У dev-середовищі це OK. Але:

- `NUTRITION_BACKUP_KEY_SECRET` не виставлений → `/api/nutrition/backup-upload`/`-download` дає 503. Якщо в `apps/web` нутриція очікує цей роут — UI спробує і впаде на failed-fetch (умовно після того, як bootstrap полагодимо).
- `BETTER_AUTH_TOKEN_ENC_KEY` хороше нагадування додати в `.env.example` як warning level, бо люди легко проґавлять у проді.

---

## 7. 🟢 Low — Engine warnings

```
WARN  Unsupported engine: wanted: {"node":"20.x"} (current: {"node":"v22.12.0","pnpm":"9.15.1"})
```

`.nvmrc` каже `20.20.2`, але VM має 22.12.0. Усе працює, але якщо `pnpm install` колись робиться з `--engine-strict`, отримаєте помилку. Достатньо `nvm install $(cat .nvmrc) && nvm use`.

---

## 8. 🟡 Latent — ще 5 однакових bombs у mobile (дзеркало #1)

Скан репо показав, що umbrella-імпорт `@sergeant/db-schema/migrate` тягне ще **п'ять** мобільних споживачів — не один:

```
apps/web/src/core/db/kvStoreBoot.ts:41          ← ЗЛОВИВ Vite (наш блокер)
apps/mobile/src/core/db/kvStoreBoot.ts:39       ← дзеркало того ж самого
apps/mobile/src/modules/finyk/lib/clientMigrate.ts:5
apps/mobile/src/modules/fizruk/lib/clientMigrate.ts:5
apps/mobile/src/modules/nutrition/lib/clientMigrate.ts:5
apps/mobile/src/modules/routine/lib/clientMigrate.ts:5
```

Web-сторона вже мігрувала всі 4 module-`clientMigrate` на безпечний `/migrate/runner` (Routine навіть із докладним коментарем-попередженням). **Mobile сторона — ні.**

Чому Metro поки не "вибухнув" так само, як Vite?

- Metro за замовчуванням **не резолвить** `node:` префікс — він транспілить `node:fs` як missing module **під час bundling**, а не runtime.
- Імпорт `loadMigrationFiles` від `files.ts` теоретично провалиться як unresolved, але Metro tree-shake-ить unused exports з re-export'ів **на рівні файлів** (named-export hoisting). Тому **поки** ніхто не зове `loadMigrationFiles` рантаймно з мобільного, проблема latent.
- Якщо хтось додасть code-path, що **тягне** будь-який експорт із `./files.ts` транзитивно — все, що exporting через umbrella, втрапить у бандл і розвалить production build. Це міна в підлозі.

**Висновок:** мобільні 5 imports — не активний краш, але **fragility debt**. Привести усі 6 (5 mobile + 1 web) до `/migrate/runner` — однорядкові правки. Це робить рефакторинг "drop the umbrella" із §10 реалістичним.

---

## 9. Чому процес ревʼю не зловив блокер #1

PR #062 / #063 / #064 пройшли всі чотири стадії (lint / typecheck / unit-test / merge), не зловивши блокер. Чому?

1. **`pnpm typecheck` не дивиться на runtime-сумісність бандлу**. Імпорт `runMigrations` з umbrella валідний типово на 100%.
2. **`pnpm test` для `@sergeant/web` ніколи не виконує real bootstrap**. У `kvStoreBoot.test.ts` (175→17 рядків після PR #064) усі залежності, включно з `runMigrations`, замокані `vi.mock()`. Реальний import-graph не виконується.
3. **`pnpm build` для apps/web не запускається в CI**. (Якщо запускався би, Vite би скрипнув: `Module "node:fs" externalized for browser`.)
4. **Smoke E2E (Cypress / Playwright) на staging — відсутній/не блокує**.

Тобто блокер пройшов через "сито" PR-revue **не тому**, що щось зламано — а тому, що захист просто **не побудований**. Усі чотири шари дозволили помилку.

---

## Sanity-check: API живий

(Манівник тест UI неможливий, але серверні endpoint'и реагують.)

```
GET  /health                 → 200
GET  /metrics                → 200
GET  /api/auth/get-session   → 200
GET  /api/finyk/transactions → 404  (endpoint може жити під іншим префіксом)
```

API запустився коректно після всіх 49 міграцій (PG: `pgvector/pgvector:pg16`).

---

## 10. Запропонований план follow-up PR-ів

Порядок за impact / leverage. Жоден з цих PR-ів не зроблений у рамках самого аудиту — це чек-ліст для подальшої роботи.

| Пріоритет | PR                                                                                                                     | Сурфейс                                                              | LOC orientation |
| --------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | --------------: |
| **P0**    | `fix(web): use migrate/runner subpath in kvStoreBoot to avoid node:fs in browser bundle`                               | `apps/web/src/core/db/kvStoreBoot.ts` (1 рядок)                      |              ~5 |
| **P0**    | `chore(ci): add vite build smoke for apps/web as hard gate`                                                            | CI workflow                                                          |             ~30 |
| P0+       | `chore(db-schema): drop umbrella ./migrate export to prevent browser regressions` (або ESLint `no-restricted-imports`) | `packages/db-schema/package.json` exports + 5 mobile imports + 1 web |             ~30 |
| P1        | `fix(mobile): unify db-schema/migrate import to /migrate/runner` (дзеркальний на 5 mobile-сурфейсах)                   | mobile                                                               |             ~10 |
| P1        | `test(mobile): renderWithProviders util to wrap ApiClientProvider — фіксить ~36 фейлів`                                | `apps/mobile/test-utils/`                                            |             ~50 |
| P1        | `test(mobile): fix orphaned identifyTraits.test.ts (delete or restore module)`                                         | mobile                                                               |             ~10 |
| P1        | `test(web): pass salt as BufferSource in lockStorage to satisfy Node22/jsdom WebCrypto`                                | `apps/web/src/core/security/lockStorage.ts`                          |              ~3 |
| P1        | `test(mobile): fix jest.mock factory hoisting for getVibePicksMock`                                                    | mobile                                                               |              ~5 |
| P1        | `test(mobile): wrap async setReduceMotion update in act() in OnboardingWizard.test`                                    | mobile                                                               |              ~5 |
| P2        | `chore(server): migrate logger/metrics/tracing/push process.env reads to env.ts` (закрити budget overflow)             | server                                                               |             ~50 |
| P3        | `chore: align node engine warning vs .nvmrc`                                                                           | repo                                                                 |              ~0 |

---

## 11. Рекомендації по CI / процесу

1. **Додати smoke-build для apps/web** у CI: `pnpm --filter @sergeant/web build` як hard gate. Це би зловило цей блокер ще на PR #062.
2. **`no-restricted-imports`** в ESLint для `apps/web/**` і `apps/mobile/**`, що рубає `@sergeant/db-schema/migrate` без саб-сегмента.
3. **Додати в playbook UX-roast** явну вимогу `pnpm --filter @sergeant/web test` перед мерджем — `useAppLock` тест ніколи не проганявся локально на Node 22.
4. **Видалити умовно-видимий PAT з git remote** (бачив у timeline під час сесії). Він не потрапив у код, але токен у URL — security smell. Раджу провернути PAT після сесії.

---

## Доказова база

### Запис

Коротке відео, де показано: URL → blank page → DevTools із вище-описаним `node:fs` помилкою:
[Recording](https://app.devin.ai/attachments/c989b966-fd70-4ae6-9550-f8dc00bfe498/rec-653255861e684fda94112ab101e85989-edited.mp4)

### Сирі логи (в межах сесії, не в репо)

- Lint: `/tmp/audit_lint.log`
- Typecheck: `/tmp/audit_typecheck.log`
- Test: `/tmp/audit_test.log` (~395 МБ — розпухло від `console.log` під час моків — підказка для потенційного `silent: true` або вимкнення console-pump у Jest)
- Web dev server: `/tmp/audit_web.log`
- API server: `/tmp/audit_server.log`

### Команди для відтворення

```bash
# 1. Bootstrap
pnpm install --frozen-lockfile
docker run --name hub-pg -e POSTGRES_USER=hub -e POSTGRES_PASSWORD=hub \
  -e POSTGRES_DB=hub -p 5432:5432 -d pgvector/pgvector:pg16
pnpm --filter @sergeant/db-schema build
pnpm --filter @sergeant/server migrate

# 2. Surface всіх регресій
pnpm typecheck      # → TS2307 (apps/mobile orphan test)
pnpm lint           # → process.env budget overflow
pnpm test           # → 48 mobile fails + 3 useAppLock fails
pnpm dev:server     # → server boot warnings (REDIS_URL etc.)
pnpm dev:web        # → відкрити localhost:5173 → blank page + DevTools

# 3. Реальна помилка PBKDF2 (ізольовано)
pnpm --filter @sergeant/web exec vitest run \
  src/core/security/useAppLock.test.ts --reporter=verbose
```

---

_Авдитор:_ Devin (session `291d0c8e`)
_Доказова база:_ `git blame`, `git log --follow`, full automated test suite + manual web-bootstrap, full grep across `apps/`+`packages/`. Без модифікацій коду.
