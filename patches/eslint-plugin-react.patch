diff --git a/lib/rules/jsx-filename-extension.js b/lib/rules/jsx-filename-extension.js
index 4a2cfeede66b68255ca48bc38c0eb2f1537a785a..8b23f390ea7c4b90c3de2db94636c349621b4ade 100644
--- a/lib/rules/jsx-filename-extension.js
+++ b/lib/rules/jsx-filename-extension.js
@@ -61,7 +61,10 @@ module.exports = {
   },
 
   create(context) {
-    const filename = context.getFilename();
+    // ESLint 10 removed `context.getFilename()`; prefer the `filename`
+    // property and fall back for older ESLint engines. See jsx-eslint/
+    // eslint-plugin-react#3972 — keep until upstream 7.38+ ships.
+    const filename = context.filename || (typeof context.getFilename === 'function' ? context.getFilename() : '');
 
     let jsxNode;
 
diff --git a/lib/util/version.js b/lib/util/version.js
index c9dc683b7d149c5a3a941bdcbd32f657facdfbf6..071dd6722d7ce6eb439c86b2828c7664497437d3 100644
--- a/lib/util/version.js
+++ b/lib/util/version.js
@@ -28,7 +28,16 @@ function resetDetectedVersion() {
 
 function resolveBasedir(contextOrFilename) {
   if (contextOrFilename) {
-    const filename = typeof contextOrFilename === 'string' ? contextOrFilename : contextOrFilename.getFilename();
+    // ESLint 10 removed the deprecated `getFilename()` method in favour of the
+    // `filename` property. Upstream eslint-plugin-react still calls
+    // `getFilename()` (see jsx-eslint/eslint-plugin-react#3972) so without
+    // this fallback the plugin throws `contextOrFilename.getFilename is not a
+    // function` on every lint of a non-React file under ESLint 10. Tracked as
+    // part of the H2/H7/C2 hardening PR — drop the patch when upstream cuts a
+    // 7.38+ release that supports ESLint 10.
+    const filename = typeof contextOrFilename === 'string'
+      ? contextOrFilename
+      : (contextOrFilename.filename || (typeof contextOrFilename.getFilename === 'function' ? contextOrFilename.getFilename() : undefined));
     const dirname = path.dirname(filename);
     try {
       if (fs.statSync(filename).isFile()) {
