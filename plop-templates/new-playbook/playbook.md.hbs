{{#if (eq lang "en")}}---
lang: en
---

# Playbook: {{humanTitle}}

> **Last validated:** {{timestamp}} by @{{owner}}. **Next review:** {{nextReview}}.
> **Status:** Active

**Trigger:** {{trigger}}

## Owner surface

- Primary surface: `apps/<app>/...` or `packages/<pkg>/...`
- Coupled surface: `<list any always-coupled paths>`
- Governing skill: `{{governingSkill}}`

---

## Steps

### 1. Read the governing skill

Open `.agents/skills/{{governingSkill}}/SKILL.md` first. The skill defines the boundary rules; this playbook is the recipe.

### 2. Reproduce or scope the change

Capture the failing behavior or describe the smallest change that satisfies the trigger.

### 3. Apply the change

Edit the owning surface only. Keep the diff small.

### 4. Update coupled surfaces

Touch all paths listed under "Coupled surface" if the change crosses a boundary.

### 5. Update governance

If this PR introduces a new convention, update AGENTS.md or the relevant ADR (Hard Rule #15).

---

## Verification

- [ ] `pnpm lint` — green
- [ ] `pnpm typecheck` — green
- [ ] `pnpm test` — green for affected packages
- [ ] Coupled surfaces inspected and updated where applicable
- [ ] Governance docs updated if a new convention is introduced

## Notes

- Stub generated by `pnpm gen new-playbook` on {{timestamp}}. Replace placeholder content before merging.
- Add this playbook to the catalog: `docs/playbooks/INDEX.md` (regenerated by `pnpm docs:gen-playbook-index`).
{{else}}
# Playbook: {{humanTitle}}

> **Last validated:** {{timestamp}} by @{{owner}}. **Next review:** {{nextReview}}.
> **Status:** Active

**Trigger:** {{trigger}}

## Owner surface

- Основна поверхня: `apps/<app>/...` або `packages/<pkg>/...`
- Coupled-поверхні: `<перерахуй пов'язані шляхи>`
- Governing skill: `{{governingSkill}}`

---

## Кроки

### 1. Прочитай governing skill

Відкрий `.agents/skills/{{governingSkill}}/SKILL.md` спершу. Skill задає межі; цей playbook — рецепт.

### 2. Відтвори або обмеж зміну

Зафіксуй поведінку, що не працює, або опиши найменшу зміну, яка закриває тригер.

### 3. Виконай зміну

Редагуй лише owner-поверхню. Тримай diff малим.

### 4. Онови coupled-поверхні

Зачепи всі шляхи зі списку "Coupled-поверхні", якщо зміна перетинає межу пакета.

### 5. Онови governance

Якщо PR вводить нову конвенцію — онови AGENTS.md або відповідний ADR (Hard Rule #15).

---

## Verification

- [ ] `pnpm lint` — зелений
- [ ] `pnpm typecheck` — зелений
- [ ] `pnpm test` — зелений на зачеплених пакетах
- [ ] Coupled-поверхні переглянуто й оновлено за потреби
- [ ] Governance-доки оновлено, якщо введено нову конвенцію

## Notes

- Заглушка згенерована `pnpm gen new-playbook` {{timestamp}}. Заміни placeholder-вміст перед merge.
- Додай цей playbook до каталогу: `docs/playbooks/INDEX.md` (регенерується через `pnpm docs:gen-playbook-index`).
{{/if}}
