-- Down migration: {{name}}
--
-- Replace this placeholder with SQL that reverses the matching `up.sql`.
-- `pnpm lint:migrations` fails on commit if this file is still empty.
-- If rollback is genuinely impossible (irreversible data write, dropping
-- an obsolete schema, etc.), delete the placeholder body and add ONE
-- comment of the form:
--   -- NO_ROLLBACK: <reason> (due: YYYY-MM-DD)

-- TODO: write your DOWN (rollback) migration here
