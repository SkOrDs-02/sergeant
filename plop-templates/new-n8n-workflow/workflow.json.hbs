{
  "name": "{{id}} — {{humanName}}",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "hours",
              "hoursInterval": 24
            }
          ]
        }
      },
      "id": "schedule-trigger",
      "name": "Daily schedule",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "jsCode": "// TODO({{slug}}): replace this stub with the real workflow logic.\n// Generated by `pnpm gen new-n8n-workflow` on {{timestamp}}.\n//\n// Conventions:\n//  - Reference env vars via dollar-env.<your_var> (uppercase) and add the\n//    name to manifest.requiredEnv (validator regex /\\$env\\.[A-Z0-9_]+/).\n//  - Reference credentials by declared name and add them to\n//    manifest.requiredCredentials.\n//  - Risk tier {{riskTier}} → keep mutations idempotent and reversible.\n//\n// See docs/playbooks/modify-n8n-workflow.md for the rollout flow.\nreturn [{ json: { ok: true, generatedAt: new Date().toISOString() } }];"
      },
      "id": "stub-code",
      "name": "Stub: replace me",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [520, 300]
    }
  ],
  "connections": {
    "Daily schedule": {
      "main": [
        [
          {
            "node": "Stub: replace me",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "Europe/Kyiv",
    "saveManualExecutions": false,
    "saveExecutionProgress": false
  },
  "tags": [
    {
      "name": "{{owner}}"
    },
    {
      "name": "{{slug}}"
    }
  ]
}
