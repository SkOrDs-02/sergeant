# @sergeant/{{slug}}

> **Last validated:** {{timestamp}} by @{{owner}}.
> **Status:** Bootstrap (generated by `pnpm gen new-package`).

{{description}}

## Що всередині

```
src/
└── index.ts            # Публічна поверхня
```

## Використання

```ts
import { {{camelCase slug}}PackageVersion } from "@sergeant/{{slug}}";
```

## Тести

```bash
pnpm --filter @sergeant/{{slug}} test
pnpm --filter @sergeant/{{slug}} typecheck
pnpm --filter @sergeant/{{slug}} lint
```

## Наступні кроки

1. Замінити stub `{{camelCase slug}}PackageVersion` на реальну поверхню пакета.
2. Додати залежності в `package.json` (через `pnpm add --filter @sergeant/{{slug}}`).
3. Оновити CODEOWNERS — `/packages/{{slug}}/` додано generator-ом, якщо власник інший — підправ вручну.
4. Якщо пакет експонує kilka entry points — додай їх в `exports` мапу `package.json`.
