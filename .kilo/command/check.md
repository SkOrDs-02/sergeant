---
description: Run full pre-PR check — format, lint, typecheck, test, build
model: openrouter/minimax/minimax-m3
---

Run `pnpm check` in the repo root.
If it fails, fix errors iteratively until `pnpm check` passes.
